# Progress Tracker — Climate Vulnerability Web-GIS

> Living status board. Each **development function** maps to its design doc, the **prompt to
> run**, the **skill/plugin** used, and whether it's **executed**. Update the Executed cell
> (☐ → ◐ → ☑) + Date + Output every time something finishes. Log problems in §3.

- **Last updated:** 2026-08-11
- **Current phase:** SRS §10 **Stage 1 — schema completion**, started 2026-08-09,
  addenda done (1.8–1.14; 1.1–1.7 remain, not started). **Stage 2 — catalogue
  and profile API — started 2026-08-11 (2.0 done): the FastAPI app now
  exists**, `backend/app/`, asyncpg + raw SQL (no ORM, no Alembic — decision
  and reasoning in the T2 row in §3), `GET /api/health` real and passing end
  to end through the Angular dev server. 2.1–2.4 (catalogue reads, profile
  read/readiness, weight save, AHP) remain. **Stage 9.6 — auth (T2b) — done
  2026-08-11, out of numeric order** ahead of Stage 2.1–2.4 because T3
  onward needs a signed-in user and a province to authorise every write
  against: landing page, self-registration, sign-in/out, admin approval
  queue, all against server-side sessions in a new table
  (`schema_session_addendum.sql`) — see the T2b row in §3.
- **Stage 5** (Angular client) started out of order, ahead of Stage 2 — see
  the 2026-08-08 "Stage 5 started" row in §3. It is still UI-only for
  everything except `/health`: every screen is real and typed against §9, but
  every other API call still fails until Stage 2.1–2.4/Stage 3 exist.
- **Authoritative spec:** `design/srs/SRS_v2.3.md`. Everything earlier is superseded.
- **Build plan:** SRS §10's **ten stages**. The B1–B8 / F1–F6 tables in §2 below and
  in `PROJECT_GUIDE.md` are **history, not a plan** — kept because the issues log
  refers to them by name.
- **Stack (locked 2026-08-08):** Angular + TypeScript · OpenLayers · FastAPI ·
  PostgreSQL + PostGIS + pgvector.

---

## 1. Status summary

Counted against the **stage board in §2a**, which is the plan of record.

| Stage | Actions | ☑ | ☐ | Gated by |
|---|---|---|---|---|
| 0 — precondition (database + data) | 5 | 5 | 0 | — **done** |
| 1 — Schema completion | 14 | 7 | 7 | — **in progress** |
| 2 — Catalogue + profile API | 5 | 1 | 4 | Stage 1 |
| 3 — Import pipeline | 7 | 0 | 7 | Stage 2 |
| 4 — Computation engine | 7 | 0 | 7 | Stage 3 · **O-9 → P-5** · O-10 at 4.6 |
| 5 — Tiles + Angular map | 8 | 0 | 8 (5 ◐) | Stage 2 (not Stage 4) |
| 6 — Spatial toolbox | 4 | 0 | 4 | Stage 1 (`coverage_geom`, `sl_apportion_2`) |
| 7 — Community + expert tracks | 3 | 0 | 3 | Stages 1, 5 |
| 8 — Scenarios, monitoring, assistant | 4 | 0 | 4 | Stage 4 · O-4 at 8.4 |
| 9 — Admin, audit, interoperability | 6 | 1 | 5 | Stage 1 (`audit_log`) |
| 10 — Migration and cut-over | 4 | 0 | 4 | Stages 2–5 |
| **Total** | **65** | **10** | **55** | |

**The honest summary: the data foundation is real and complete; the application
layer does not exist.** Phase 0 design work is done bar D4, whose remaining
screens are now Stage 5 and Stage 7 work.

**Now / next:** ☑ D3 schema · ☑ **Catalog finalized (2026-07-26)** — expert-refined
`Variables by Sectors` supersedes the provisional dedup; **174 canonical variables, 33
profiles** (`design/ingestion/FINAL_VARIABLES.xlsx` + `seed_all.sql`).
☑ **D6 — all 243 templates generated** (`templates/generated/`), structurally verified,
legacy weights carried forward where variables survived (51% of slots).
☑ **A1 + A4 done (2026-07-26)** — official shapefiles received; **330 real DS divisions with
province and district** now in every template (17,826 data rows), rows grouped by district.
Full ADM1/ADM2/ADM3 hierarchy validated, topology clean, all layers staged in `design/spatial/`.
☑ **Weights flow decided & built (2026-07-26)** — captured in the app at import and editable
any time from **Profile weights**; saves take effect immediately (**no approval gate in V1** —
expert-panel review happens offline and is recorded as a note). Every save is versioned so
published scores stay explainable. The 1,783 blank slots no longer block distribution.
☑ **D8 spatial model done (2026-07-26)** — `spatial-model.sql` (5 tables, EPSG:5235 helpers).
◐ D4 prototype now runs on the real catalog.
☑ **A3 year-range rules decided (2026-07-26)** — *latest period wins* + *typical year, not a
total*; wired through schema, all 243 templates and `_META`. 174 variables classified
(160 average · 11 fixed_window · 3 max) — expert sign-off pending on 14 exceptions only.
☑ **ERD regenerated from the DDL (2026-07-26)** — 28 tables incl. D8; `generate_erd.py`
replaces the hand-written .dot so it cannot drift again.
☑ **B1 database BUILT and verified (2026-07-29)** — see the issues log; the first real run
exposed 6 bugs. ☑ **D1/D2 diagrams done.**
☑ **D5 SRS done (2026-07-29); v1.1 · v1.2 · v1.3 (2026-07-31)** — 36 pp for GGGI: real
DS-division maps, GIS/toolbox section, 17-screen prototype annex; every quantitative claim
machine-checked against source data. **v1.1 folds in the review of the deployed system**
at `riskradar.geoinfobox.com` — new §1.5, §5.5, §6.11, Annex A.0 and Annex D; §6.5 rewritten
around the assessed / pending / unassessed distinction.
**Phase 0 is complete except D4** (profile composer + mapping resolver screens).
**Nothing blocks data entry, and nothing blocks B3.**
See `FINAL_VARIABLES.xlsx → Issues_to_confirm` for the open confirmations.

---

## 1a. Open actions — what remains

> **Superseded 2026-08-08.** The live open-item register is **SRS v2.3 §11.3
> (O-1 … O-10)**, and the provisional decisions taken so the build is not
> blocked are **Annex C (P-1 … P-14)**. This section is kept because its A/N
> identifiers appear in §3 rows; every row below is either resolved or restated
> there. The one that matters today is **O-9**.

Ordered by what unblocks the most. "Owner" = who has to decide, not who types.

### Blocking (work cannot proceed until these land)

| # | Action | Owner | Blocks | Where |
|---|---|---|---|---|
| ~~A1~~ | ~~Official DS-division register~~ — **DONE 2026-07-26.** 330 real DS divisions now in every template; shapefiles staged in `design/spatial/` | — | — | `design/ingestion/dsd_register.csv` |
| A2 | Fill **weights for the 1,783 new variable slots** (51% inherited a legacy weight; the rest are variables experts newly added). **No longer a blocker** — weights are captured in the app at import, saved immediately, and editable any time (2026-07-26 decision) | Officers at import, after local expert-panel sign-off | Stage 4 (was B3) — now tracked as **O-1** | app: *Data import → Confirm weights*, or *Profile weights*; optional offline pre-fill on the workbook `WEIGHTS` tab |
| ~~A3~~ | ~~Year-range rules~~ — **DECIDED 2026-07-26.** Boundary year: **latest period wins**. Range value: **a typical year, not a multi-year total**. Both wired into schema, templates and `_META`. Remaining: expert sign-off on the 14 per-variable exceptions | Expert panel (review only) | nothing — B3 can be built against these rules | `design/ingestion/PERIOD_RULES_REVIEW.xlsx` |
| ~~A4~~ | ~~District (ADM2) layer~~ — **DONE 2026-07-26.** `SL_DSD.shp` (25 districts) received; all 330 DS divisions now carry their district | — | — | `design/spatial/SL_DSD.shp` |

### Confirmations (defaults applied — say the word to reverse)

| # | Question | Default I applied | Where |
|---|---|---|---|
| C1 | "Paddy **Food**" / "Tea Food" / "Vegetable & OFC Food" in the sector list | Read as **Flood** | fix at source in `1_Sectors_list.xlsx` |
| C2 | Is "total livestock **operators**" the same denominator as "total livestock **farmers**"? | Standardised all six species on `_LIVESTOCK_OPERATORS`; old codes kept as aliases | `FINAL_VARIABLES.xlsx → Crosswalk` |
| C3 | **Rubber × Flood** — declared but no variable sheet ("island wide only") | Excluded; no DSD-level profile, no template | — |
| C4 | **Vegetables & OFC** and **Inland Fishery** have legacy data for **Central only** (1 of 9 provinces) | Generated Central only. If collection is merely incomplete, I extend to all 9 (+16 workbooks) | `MANIFEST.csv` |
| C5 | 8 retired variables (4 CBO-connection, safe-well, 2 NWSDB-domestic splits, ambiguous monthly milk) | Retired, aliased | `FINAL_VARIABLES.xlsx → Crosswalk` |
| C6 | Embedding dimension for `agent_embedding` | `vector(1024)` placeholder until the self-hosted model is chosen | `schema.sql` |

Full list with a **your decision** column: `design/ingestion/FINAL_VARIABLES.xlsx → Issues_to_confirm` (14 rows).

### Next build steps (not blocked — can start now)

| # | Action | Note |
|---|---|---|
| ~~N1~~ | ~~**D8** — spatial layer + toolbox tables~~ — **DONE 2026-07-26**, ERD regenerated | `design/database/spatial-model.sql`, `erd.svg/.png`, `generate_erd.py` |
| ~~N2~~ | ~~**D1 / D2** diagrams~~ — **DONE 2026-07-29** via Graphviz, generated not hand-drawn | `design/architecture/generate_diagrams.py` |
| ~~N3~~ | ~~**D5** — SRS~~ — **DONE 2026-07-29.** 36 pp for GGGI: 4 real DS-division maps, expanded GIS/toolbox section, 17-screen prototype annex | `design/srs/SRS.docx` |
| N4 | **D4** — UI inventory + prototype | Must include the profile-composer and mapping-resolver screens (D3 rev 5) |
| ~~N5~~ | ~~**B1** — database~~ — **BUILT AND SMOKE-TESTED 2026-07-26 → 2026-07-29.** Native route run end-to-end against a pre-existing local PostgreSQL 15 + PostGIS server: `.\db\check_prereqs.ps1 && .\db\apply_native.ps1 -Reset && .\db\smoke_test_native.ps1`. FastAPI app still to scaffold | `backend/README.md` |

---

## 2. Development functions

Legend: ☐ pending · ◐ in progress · ☑ executed

### Phase 0 — Design

| # | Development function | Design doc / folder | Prompt to give | Skill / plugin | Executed | Date | Output |
|---|---|---|---|---|---|---|---|
| D1 | System architecture diagram | `design/architecture/` | "Create a system architecture diagram… (PROJECT_GUIDE §4 D1)" | Graphviz (Figma not authorised) | ☑ | 2026-07-29 | `system-architecture.svg/.png/.dot` via `generate_diagrams.py` — 6 layers, built-vs-planned shown by border style |
| D2 | Data-flow diagram | `design/architecture/` | "Create a data-flow diagram… (D2)" | Graphviz (Figma not authorised) | ☑ | 2026-07-29 | `data-flow.svg/.png/.dot` — raw → normalise → weight → indices → vulnerability → SSP → monitoring, with the A3 period rules and the readiness gate drawn in |
| D3 | Database ERD + schema.sql | `design/database/` | "Design the PostgreSQL+PostGIS+pgvector schema… (D3)" | Graphviz ERD + manual DDL | ☑ | 2026-07-02 | `schema.sql` (rev 2), `erd.svg/.png/.dot` |
| D4 | UI screen inventory + wireframes | `design/ui/` | "List every screen… then build a clickable prototype… (D4)" | web-artifacts-builder | ◐ | 2026-07-26 | `risk-radar-ui-prototype.html` — real catalog data (330 DSDs, 174 vars, 243 profiles); upload→confirm-weights→save flow + **Profile weights** editor with change history, jsdom-verified (0 errors). Still to add: profile-composer, mapping-resolver, impact explorer, toolbox |
| D5 | SRS / design document | `design/srs/` | "Co-author a Software Requirements Specification… (D5)" | markdown → pandoc → docx | ☑ | 2026-07-29 | `SRS.md` + `SRS.docx` (36 pp, 8.5 MB) — 56 functional + 9 non-functional requirements, 24 figures incl. **4 real DS-division maps**, expanded GIS/toolbox section, 17-screen prototype annex. `build_docx.py` + `generate_maps.py` regenerate |
| D6 | Excel upload templates | `design/templates/` | "Create two Excel upload templates… (D6)" | xlsx + generator script | ☑ | 2026-07-26 | **All 243 workbooks** in `templates/generated/<Province>/` + `MANIFEST.csv`; rebuilt `generate_templates.py` driven by `FINAL_VARIABLES.xlsx` + `dsd_register.csv` |
| D7 | Per-sector ingestion (loop) | `design/ingestion/` | "Ingest into the database: Sector = …, Subsector = …, using uploaded Excel… (playbook)" | xlsx + pdf reading | ☐ | | runs per sector |
| D8 | Spatial layer + toolbox model | `design/database/` | "Design the spatial layer + analysis toolbox tables… (D8)" | Graphviz ERD + manual DDL | ☑ | 2026-07-26 | `spatial-model.sql` — 5 tables + 3 types, metric helpers in EPSG:5235, `indicator_value.computation_job_id` lineage, 4 layers + 6 operations seeded. **ERD regenerated** (`erd.dot/.svg/.png`, 28 tables) |

> The Phase 1–4 tables that used to sit here (B1–B8, F1–F6, A1–A3, M1–M3) were
> **replaced on 2026-08-08** by the stage board in §2a. They described a plan
> that no longer exists. The old identifiers survive only inside §3 rows written
> before that date; read them there as history. `B1` maps to Stage 1's
> precondition — the database — which is built.

---

## 2a. Stage board — SRS §10

**The plan of record.** Ten stages, each naming what must be true before the
next begins. Nothing here has started: the database is built and smoke-tested,
and **nothing above it exists**.

Legend: ☐ pending · ◐ in progress · ☑ done

### Stage 0 — precondition (done)

| Item | State |
|---|---|
| Schema, 28 tables, negative tests passing | ☑ 2026-07-29 |
| Reference + catalogue data seeded — 9 provinces · 3 hazards · 8 sectors · 12 subsectors · 174 variables · 269 aliases · 243 profiles · 3,664 memberships | ☑ 2026-07-29 |
| 330 DS divisions loaded, topology validated, 65,976.7 km² | ☑ 2026-07-29 |
| Spatial toolbox schema + 6 operations | ☑ seeded, never exercised |
| 243 collection workbooks, 17,826 rows | ☑ 2026-07-26 |

### Stage 1 — Schema completion ☐ **← next**

*Nothing else can be tested against an incomplete schema, so this comes first.*
Goes in an **addendum**, not by editing `schema.sql` (working rule 4).

| # | Action | State |
|---|---|---|
| 1.1 | Add `community_rating` — a severity rating is not an indicator value; no `indicator_id`, keyed on sector × hazard | ☐ |
| 1.2 | Add `audit_log` — append-only, no update or delete path | ☐ |
| 1.3 | Add `nap_sector` + `sector_nap_map` (crosswalk, `is_assessable` flag) | ☐ |
| 1.4 | Add `spatial_layer.coverage_geom` (FR-6.2, FR-6.9) | ☐ |
| 1.5 | Add `sl_apportion_2()` for two-layer intersection (FR-6.5) | ☐ |
| 1.6 | Fix `iv_for_year()` ordering for determinism (§8.4) | ☐ |
| 1.7 | Re-seed periods non-overlapping — to 2025, then from 2026 (P-7) | ☐ |
| 1.8 | Regenerate the ERD (`generate_erd.py`) | ☑ 2026-08-09 |
| 1.9 | Re-run the semantic checker over all DDL — parsing is not verification (working rule 2) | ☑ 2026-08-09 — now a kept script, `design/database/check_ddl.py` |
| 1.10 | Add `profile_indicator.consensus` (+ note, dissent count, decider) — disagreement about whether a variable belongs is not expressible as a weight | ☑ 2026-08-09 |
| 1.11 | Add `vulnerability_profile.publication` (published / draft / sandbox) + owner + `derived_from_id`; one published active version per scope | ☑ 2026-08-09 |
| 1.12 | Apply `schema_consensus_addendum.sql` to the live database and run the negative tests | ☑ 2026-08-09 |
| 1.13 | Apply `schema_auth_addendum.sql` — registration, approval, **enforceable provincial scope**; run the negative tests | ☑ 2026-08-09 |
| 1.14 | Apply `schema_boundary_pending_addendum.sql` — the Kalmunai split, 330 → 331 (O-2); nullable `geom`, `boundary_status`, no score for a boundary-pending division; run the negative tests | ☑ 2026-08-10 |

**Exit:** every §8.3 constraint has a passing **negative** test — an attempt that
should fail, failing for the stated reason. 1.1–1.7 remain — schema completion is
not done, only its two-then-three addenda are.

### Stage 2 — Catalogue and profile API ◐  *(first FastAPI code)*

| # | Action | State |
|---|---|---|
| 2.0 | Scaffold the FastAPI application — this has never been done | ☑ 2026-08-11 |
| 2.1 | Read endpoints: indicators, sectors, subsectors, hazards, NAP crosswalk | ☐ |
| 2.2 | Profile read, including readiness (`v_profile_readiness`) | ☐ |
| 2.3 | Weight save via `save_profile_weights()`, versioned, with history | ☐ |
| 2.4 | AHP endpoint returning the consistency ratio | ☐ |

**Exit:** a weighting can be saved, **rejected at 99.999**, versioned, and its
history read back.

### Stage 3 — Import pipeline ☐

| # | Action | State |
|---|---|---|
| 3.1 | Workbook generation from a profile | ☐ |
| 3.2 | Upload, full validation against Annex A, staging | ☐ |
| 3.3 | Weights read from the WEIGHTS tab and returned for confirmation — screen authoritative, file advisory | ☐ |
| 3.4 | Atomic load, batch rollback | ☐ |
| 3.5 | Alias reconciliation for unrecognised headers | ☐ |
| 3.6 | Granularity validation (FR-2.7) | ☐ |
| 3.7 | Province import summary — loaded, computed, outstanding (FR-2.12–2.16) | ☐ |

**Exit:** one fixture workbook per validation code, each producing its stated
error and **loading nothing**.

### Stage 4 — Normalisation and computation engine ☐ **gated on O-9 → P-5**

| # | Action | State |
|---|---|---|
| 4.1 | National-bounds normalisation — three methods, three temporal scopes | ☐ |
| 4.2 | Direction inversion | ☐ |
| 4.3 | Period resolution and carry-forward, **with marking** | ☐ |
| 4.4 | Hazard and exposure indices, then `V_raw = H × E` | ☐ |
| 4.5 | Provincial renormalisation, storing the bounds used (P-14) | ☐ |
| 4.6 | Banding, thresholds read from configuration (blocked on O-10) | ☐ |
| 4.7 | Refusal on unweighted variables, naming which | ☐ |

**Exit:** recomputation bit-identical; a profile with one unweighted variable
refuses and names it; a division outside a variable's data returns
**unassessed, not zero**; every province produces exactly one 1.000 and one
0.000 per profile — the signature that 4.5 ran.

### Stage 5 — Tiles and the public map ☐  *(may start once Stage 2 lands)*

Stands up the **Angular** client (SRS §4.1). Everything the frontend does later
builds on this scaffold.

| # | Action | State |
|---|---|---|
| 5.1 | Angular scaffold: routing, OpenLayers map component, typed API client checked against §9, environment config with **no hardcoded API host** | ◐ 2026-08-08 |
| 5.2 | Vector tile generation with per-zoom simplification | ☐ — stopgap in place, see §3 |
| 5.3 | Choropleth with the three coverage states rendered distinctly — **NFR-10, an absent value is not a zero** | ◐ 2026-08-08 — states render, no test asserting it yet |
| 5.4 | Sector, subsector, hazard, province, track and period controls | ◐ 2026-08-08 |
| 5.5 | Score composition panel | ◐ 2026-08-08 — panel exists, empty until Stage 2 |
| 5.6 | Coverage statement and coverage screen | ◐ 2026-08-08 — statement done, screen still a stub |
| 5.7 | Ranking chart and value table | ☐ |
| 5.8 | Shareable URL state, print output, error and loading states | ◐ 2026-08-08 — loading/error done, URL state and print still open |

**Exit:** NFR-3 timings met at national extent; no view renders an absent value
as zero, **asserted by an automated test, not by looking at the map**.

### Stage 6 — Spatial toolbox ☐

| # | Action | State |
|---|---|---|
| 6.1 | Layer registration with attribute contract and coverage extent | ☐ |
| 6.2 | The six single-layer operations, then the two-layer operation | ☐ |
| 6.3 | Job queue, review on map, commit with job linkage | ☐ |
| 6.4 | Extent-aware zero handling (FR-6.9) | ☐ |

**Exit:** a committed value traces back to layer, operation and parameters; a
division outside a layer's extent is unassessed.

### Stage 7 — Community and expert tracks ☐

| # | Action | State |
|---|---|---|
| 7.1 | Expert value entry — map-select → side panel, multi-select (§6.2). Interaction carries over from the retired React client; its code does not | ☐ |
| 7.2 | Community rating with the uniqueness key and session flagging | ☐ |
| 7.3 | Three-track comparison view with divergence listing | ☐ |

### Stage 8 — Scenarios, monitoring, assistant ☐

| # | Action | State |
|---|---|---|
| 8.1 | SSP scenarios and projection, stored **separately from observations** | ☐ |
| 8.2 | Impact explorer | ☐ |
| 8.3 | Monitoring dashboard — predicted vs actual | ☐ |
| 8.4 | Document indexing, retrieval-augmented answers, expert review workflow (blocked on O-4; needs `schema_agent_pgvector.sql` applied) | ☐ |

### Stage 9 — Administration, audit, interoperability ☐

| # | Action | State |
|---|---|---|
| 9.1 | User, role and provincial scope management; registration approval | ☐ |
| 9.2 | Catalogue governance | ☐ |
| 9.3 | Audit log write path on **every** mutating endpoint, plus the query interface | ☐ |
| 9.4 | OGC services (GeoServer) and exports | ☐ |
| 9.5 | Health endpoint and observability | ☐ |
| 9.6 | Auth: landing page, sign-in, self-registration, server-side sessions (T2b) | ☑ 2026-08-11 |

### Stage 10 — Migration and cut-over ☐

| # | Action | State |
|---|---|---|
| 10.1 | Reference-data reconciliation with an unmatched report | ☐ |
| 10.2 | Archived historical results with `pre-migration` origin — composition recorded as unknown, not faked | ☐ |
| 10.3 | GND-level preservation | ☐ |
| 10.4 | Reconciliation report, **accepted before cut-over** | ☐ |

### Not in the ten stages

| Item | Why |
|---|---|
| Risk layer `Risk = f(H, E, V)` | Specified in §6.9, **deferred to phase 2** (P-12). Needs asset layers (O-7) and Stage 6's two-layer operation |
| D4 prototype completion | The prototype was a design instrument. Its unbuilt screens are now Stage 5 and Stage 7 work, in Angular |
| Security review / CI (old M2) | Folded into Stage 9 and NFR-4b–4d, not a separate late phase |
| Stakeholder deck (old M3) | Delivery artefact, not a build stage |

### What gates what

- **Stage 4 cannot complete until O-9 is answered.** It settles P-5, and P-5
  explains all 558 hazard-domain blanks. Ask the panel **now**, not at Stage 4.
- **Stage 5 can begin before Stage 4 finishes** — coverage states are testable
  with no scores at all.
- **Stage 6's two-layer operation** is required before the fourteen intersection
  variables (P-11), and before any phase-2 risk work.
- **Stage 8's assistant must degrade without affecting anything else**
  (FR-10.3), so it can safely be sequenced last.
- **O-1** (1,783 unweighted memberships) limits *which* profiles Stage 4 can
  compute, not whether the engine works. **O-8** and **O-10** bite at 4.6.
  **O-4** blocks 8.4 only. **O-6** blocks NFR-12.

---

## 3. Issues & solutions log

| Date | Component | Issue encountered | Resolution / decision | Status |
|---|---|---|---|---|
| 2026-06-20 | D3/D8 | Risk of per-sector & per-layer tables exploding the schema | Adopted config-driven catalogs (indicator_catalog, spatial_layer) — heterogeneity as data, not tables | Resolved |
| 2026-06-20 | D8 toolbox | Area/density indices wrong if computed in WGS84 degrees | Compute in EPSG:5235 (SLD99); store geom in 4326 for display | Resolved |
| 2026-06-20 | Tracks | Three tracks could drift if each normalizes differently | Normalize server-side from raw values; tracks share catalog, differ by `source` | Resolved |
| 2026-07-02 | D3 ERD | Figma connector not authorized in session | Generated ERD with Graphviz (svg/png + editable .dot source); guide already allowed non-Figma fallback | Resolved |
| 2026-07-02 | D3 schema | Embedding dimension unknown (self-hosted model TBD, §5 open question) | `agent_embedding.embedding vector(1024)` placeholder — adjust when model chosen | Open |
| 2026-07-02 | D3 rev 2 | Paddy sample (Eastern prov.) shows variable sets + weights are per sector×hazard, weights contextual, variables reused across hazards | Added weighting layer: `vulnerability_profile` + `profile_indicator` (weight_pct, +/- relationship, versioned, national scope); `indicator_catalog` → pure variable registry (weight removed); `vulnerability_result.profile_id` lineage | Resolved |
| 2026-07-02 | Sample data | Flood sheet has duplicate label E7 (% forest cover AND paddy production) | Catalog codes must be globally unique variable codes; sheet labels (E1/H1) kept as display labels on profile_indicator | Resolved |
| 2026-07-02 | D3 rev 3 | Per-year min–max makes vulnerability incomparable across years (temporal variation misleading) | Added `indicator_catalog.norm_scope` (pooled default / per_year / fixed_bounds); engine rule: carry latest value forward for missing years | Resolved |
| 2026-07-02 | D3 rev 4 | 9-province workbooks (243 files parsed): same sector×hazard has different variables/weights per province — reverses rev 2 "national scope" decision | Added `province` table; `province_id` on ds_division + vulnerability_profile (NULL = national fallback); unique key + scope index updated | Resolved |
| 2026-07-02 | D3 rev 5 | Legacy datasheets won't match templates; hard rejects would block ingestion, but open registration would duplicate variables | Dual-path ingestion: strict (templates) + reconciliation wizard; added `indicator_alias` (remembered mappings) and `indicator_catalog.status` pending/active/retired + `proposed_by` (admin approval gate); D4 gains profile-composer + mapping-resolver screens | Resolved |
| 2026-07-18 | D6 yearly layout | Yearly data collection: layout decided — one Data tab per year (2020–2024), YEAR pre-filled+locked per tab, importer cross-checks tab name vs YEAR column; skip a tab if no data for that year | Templates regenerated with 5 year-tabs each | Superseded by rev 6 |
| 2026-07-26 | **A3 year-range rules RESOLVED** | Two gaps open since 2026-07-18: (a) 2025 sits in **both** period tabs, so a query for 2025 had two candidate answers and any naive sum double-counted it; (b) nothing said whether a range value was a **six-year total** or a **typical year** — a factor-of-six difference. Risk was not a crash but a **quietly wrong, believable map**: if one province entered totals and another averages for the same variable, normalisation would read the first as ~6× more exposed | **Decisions (Milinda):** boundary = **latest period wins**; range value = **typical year**. Chosen because a typical year is the only reading valid for both stocks (population, extent, %) and flows (production, events) — summing a stock over six years inflates it sixfold. Implemented: `period_aggregation` enum + column on `indicator_catalog`; `iv_for_year()` resolves the boundary with `ORDER BY year_start DESC LIMIT 1`; per-column instruction printed in every workbook sub-header; rule + full per-variable map carried in hidden `_META` so the importer can validate; README states both rules. All 243 regenerated and verified | **Resolved** |
| 2026-07-26 | Per-variable exceptions | A blanket "typical year" is wrong for some variables | Classified all 174: **160 average · 11 fixed_window · 3 max**. `fixed_window` = the variable defines its own year window (`FLOOD_EVENTS_1974_TO_2023`, the three hazard indices, SPI, TX90p…) so the same value belongs on both tabs; `max` = `MAXIMUM_*_AFFECTED_PEOPLE`, already an aggregate. Machine-readable in `period_aggregation.json/.csv`; review sheet `PERIOD_RULES_REVIEW.xlsx` (14 non-average rows highlighted, hazard-domain rows flagged for a closer look) | Open — expert review only, nothing blocked |
| 2026-07-18 | D3 rev 6 + D6 | Expert input: datasets cover YEAR RANGES (2020–2025, 2025–2030), not single years | Schema: `year` → `year_start`/`year_end` in indicator_value, vulnerability_result, impact_projection (single year = equal values); templates: one tab per period, locked YEAR_START/YEAR_END. Engine rule for shared boundary year (2025 in both): latest period wins — CONFIRM with expert. Also confirm per variable: range totals vs range averages | Resolved (2 confirmations pending) |
| 2026-07-18 | Catalog dedup | 311 raw descriptions → 265 normalized → **213 proposed canonical variables** (token-level fuzzy clustering; first pass over-merged cattle/buffalo etc. — fixed) | `catalog_dedup_review.xlsx` 62 CHECK rows — **no longer needs manual review**: superseded by the expert-refined list (see 2026-07-26 row). Kept as the alias source for legacy sheet labels | Superseded 2026-07-26 |
| 2026-07-18 | D6 prototype | DS-division lists and catalog codes not yet official | Sample templates used placeholder DSD lists (8/province) + provisional slug codes | Superseded 2026-07-26 — codes now official; DSD placeholder issue carried forward to the 2026-07-26 "DS divisions" row |
| 2026-07-26 | Catalog FINAL | Expert-refined `Variables by Sectors` (14 workbooks) received — supersedes the 213-variable provisional dedup. Experts assigned official codes per sector×hazard | **174 canonical variables, 33 profiles, 463 profile-variable rows.** Crosswalk: 129 unchanged, 76 renamed, 8 retired, 4 new. Fixed 2 malformed codes (`PCT_PADDY_LAND_TOTAL _LAND_AREA` space; 74-char fish-processing code). Outputs: `FINAL_VARIABLES.xlsx`, `seed_indicator_catalog_final.sql` | Resolved — supersedes 2026-07-18 dedup row |
| 2026-07-26 | Sector list | `1_Sectors_list.xlsx` hazard labels read "Paddy **Food**", "Tea Food", "Vegetable & OFC Food" | Typo for "Flood" — treated as Flood; fix at source | Open (confirm) |
| 2026-07-26 | Livestock denominators | Buffalo/Cattle/Goat used `..._LIVESTOCK_OPERATORS`; Pig/Sheep/Poultry used `..._LIVESTOCK_FARMERS` | **Applied:** standardised all six on `_LIVESTOCK_OPERATORS` (majority form); old codes retained as aliases so it is reversible. Still needs expert confirmation that the denominator is genuinely the same | Applied — confirm |
| 2026-07-26 | Coverage | Rubber × Flood declared in sector list but no variable sheet supplied | Note says "Analyzed for Island wide only" — no DSD-level profile created | Open (confirm) |
| 2026-07-26 | Weights | Refined list defines WHICH variables per profile but carries no weight % | Legacy weights carried forward from the 9-province workbooks where the variable survived — **1,881 of 3,664 slots (51%)** pre-filled; hazard-domain weights already sum to 100 in all 243. Remaining 1,783 slots are NEW variables: sector teams fill `CONFIRMED WEIGHT %` on each workbook's WEIGHTS tab (live SUM check per domain) | Open — blocks vulnerability compute (B3) |
| 2026-07-26 | D6 regenerate | 243 templates rebuilt against the final catalog | `generate_templates.py` rewritten: reads `FINAL_VARIABLES.xlsx` (codes + membership), `dsd_register.csv` (rows), `variables_inventory.csv` (province coverage), `legacy_weight_map.json` (weight carry-forward). Adds WEIGHTS tab + DISTRICT column; supports `--resume`. All 243 verified: sheet set, header vs `_META.expected_columns`, variable set vs catalog, cell protection, SUM formulas | Resolved |
| 2026-07-26 | DS divisions | No official DS-division register yet | Rows externalised to `design/ingestion/dsd_register.csv` so the register is a one-file swap | Resolved same day — see next row |
| 2026-07-26 | **A1 RESOLVED** | Official shapefiles received (`SL_RDSD` = 330 DS divisions/ADM3, `SL_PD` = 9 provinces/ADM1, `SL_GND` = 14,019 GNDs/ADM4). **None carried parent columns** — DSD file has only `ADM3_EN`/`ADM3_SI` | Derived province per DSD by **max-area spatial overlap** against `SL_PD`. Result: all 330 assigned, **zero straddling** (every DSD ≥95% inside one province). Topology validated: 0 invalid geometries, 0 overlapping pairs, DSDs tile the provinces to within 0.054% (coastal slivers). All 330 DSD names are **unique nationally** → safe join key. Generated `ds_code` (`CEN-001`…) alphabetically within province since no official code exists. Shapefiles staged in `design/spatial/` | **Resolved** |
| 2026-07-26 | GND layer | `SL_GND.shp` (14,019 polygons) supplied first — wrong admin level (ADM4, not ADM3) and no parent columns; 935 GND names are reused nationally (e.g. "Medagama" ×19) so names alone cannot identify a division | Not used for the register. Retained as a candidate finer-grain layer for later spatial analysis (D8/B8) if needed | Parked |
| 2026-07-26 | Districts | Neither `SL_PD` (ADM1) nor `SL_RDSD` (ADM3) carries a district field | **Resolved same day:** `SL_DSD.shp` (ADM2, 25 districts) supplied. Spatial join assigned all 330 DSDs a district with **zero ambiguity**; province-via-district matches province-direct for all 330, and all 25 districts are used. Template rows now **ordered by district** within each province so collectors work district by district. `ds_code` re-issued in that order (nothing had been entered yet) | **Resolved** |
| 2026-07-26 | DSD count | Shapefile has **330** DS divisions; the figure usually quoted for Sri Lanka is 331 | Difference is one division — likely a boundary revision or a merged/split DSD in this vintage. Worth confirming against the source register before the data is treated as authoritative | Open (confirm) |
| 2026-07-26 | Coverage gap | Vegetables & OFC and Inland Fishery have legacy data for **Central province only** (1 of 9) | Templates generated for Central only, per legacy coverage. Is this real, or is collection simply incomplete elsewhere? | Open (confirm) |
| 2026-07-26 | **Weights → captured in the app** | Weights were to be pre-filled in 243 workbooks, leaving 1,783 blank slots blocking B3 | **Decision (Milinda):** weights are entered while importing the Excel, per sector-vulnerability profile. Implemented: officer uploads workbook first → importer reads its `WEIGHTS` tab → **Confirm weights** screen pre-filled and **editable every time**, live Σ per domain, blocks save until each domain totals 100 with no blanks. Excel `WEIGHTS` tab kept **visible but reference-only** — the app is master | **Resolved** |
| 2026-07-26 | **No approval gate in V1** | First cut had officer *proposes* → expert *approves* in-app | **Decision (Milinda): drop it — don't complicate V1.** All data is verified against a **local expert panel offline**; once the panel signs off, the data feeder/admin edits and saves directly. No pending states, no hard rules. Proposal tables and the approvals queue were removed; replaced by a **Profile weights** screen (province × sector × subsector × hazard → edit → Save) open to officer, expert and admin, plus a free-text **panel sign-off note** captured at save so the offline review is still recorded | **Resolved** |
| 2026-07-26 | Weight versioning | Overwriting weights would silently invalidate every past `vulnerability_result` — a published score could no longer be explained | Kept, because it is invisible to the user: one button, one save. `save_profile_weights()` writes a **new `vulnerability_profile` version** instead of mutating `profile_indicator`; a **change-history** table on the weights screen shows version, who saved, date, source file and panel note. Deliberately cheap to reverse — drop the function and UPDATE in place if it ever gets in the way. Also added the missing **`import_batch`** table (schema.sql had no handle for "which file did this come from") and `indicator_value.import_batch_id` so a bad import can be undone as a unit. See `design/database/schema_weights_addendum.sql` | Resolved |
| 2026-07-26 | D8 spatial model | Toolbox could write straight into `indicator_value`, blurring "someone ran a calculation" with "this is an official number"; and area/density computed in 4326 would come out in degrees | `spatial-model.sql`: `spatial_layer` (national attribute contract in JSONB, validated by trigger), `spatial_feature` (GIST + GIN indexed, geometry type enforced against the layer), `spatial_operation`, `computation_job`, `computation_result`. Toolbox writes **results**, which a user explicitly **commits**; `indicator_value.computation_job_id` + a CHECK make a `derivation='computed'` row *impossible* without a job. Helpers `sl_area_km2()` / `sl_length_km()` / `sl_apportion()` all transform to **EPSG:5235** first. `spatial_feature.ds_division_id` left **nullable** on purpose — a road or river crosses divisions, so per-division apportionment is computed, not stored. Seeded 4 layers (LULC/water/roads/buildings — matching the map toggles) + 6 operations | Resolved |
| 2026-07-26 | pgvector blocked a native Windows install | Milinda has no Docker and pgvector has **no official Windows binary** (needs Visual Studio build tools). `schema.sql` did `CREATE EXTENSION vector` and declared `agent_embedding.embedding vector(1024)`, so the whole 18-table core failed to build over 4 tables that belong to **Phase 3** | Split the agent RAG tables into `design/database/schema_agent_pgvector.sql`. `schema.sql` is now 18 tables with **zero** vector references and needs only PostGIS. `apply_native.ps1` probes for pgvector and skips that one file when absent, saying so. Run it later when the agent layer starts. Also added a full native-Postgres route (`apply_native.ps1`, `load_spatial.ps1`, `smoke_test_native.ps1`) so Docker is optional | Resolved |
| 2026-07-26 | **Seed SQL was broken** | Building B1 for real exposed that `seed_indicator_catalog_final.sql` **did not match `schema.sql` at all**. It inserted `vulnerability_profile.sector/subsector/hazard/status` (text) and `profile_indicator.indicator_code` — none of which exist; the real columns are `sector_id`/`subsector_id`/`hazard_type_id`/`is_active` and `indicator_id`. It had parsed cleanly for weeks because **syntax checking never resolves column names**. Also had no reference data at all: no provinces, hazards, sectors or subsectors to point its foreign keys at | Replaced by **`seed_all.sql`**, generated by `generate_seed.py` from the same artefacts the templates use. Resolves every FK by *code* in a subquery, so it is order-independent and re-runnable. Now seeds 9 provinces · 3 hazards · 8 sectors · 12 subsectors · 174 variables · 269 aliases · 243 profiles · 3,664 memberships. Old file kept with a DO-NOT-RUN banner. Semantic checker extended to verify NOT NULL columns are supplied, not just that named columns exist | **Resolved** |
| 2026-07-26 | `weight_pct NOT NULL` blocked the seed | `profile_indicator.weight_pct` was `NOT NULL CHECK (> 0)`, written before the decision that weights are entered in the app. Seeding was therefore impossible: 1,783 of 3,664 memberships are variables the expert refresh added and legitimately have no weight yet | Addendum rev 4 drops the NOT NULL. `NULL` now means "belongs to the profile, weight not set" — exactly what the UI renders as *new — needs weight*. CHECK still applies when a weight is present. Added `v_profile_readiness` so an admin can see which of the 243 profiles are computable and B3 can refuse to compute a profile with missing weights | Resolved |
| 2026-07-29 | SRS had no real maps | Milinda's review: *"I cannot see any polygons of DS divisions, and where is the web-GIS analysis?"* Fair. The prototype draws a **schematic tile grid**, not real geometry, so the screen annex left a reader unable to see this is a GIS at all — and the toolbox appeared only as five lines of requirement text | Added `generate_maps.py`, rendering **4 figures from the actual 330-polygon shapefile**: divisions by province, ADM1/2/3 nesting, area per division, and a worked `DIST_TO_NEAREST` toolbox output. Expanded §6.7 with the six seeded operations, why derived indicators need them, and why the two-step commit exists. Annex A2 now states plainly that the prototype's tiles demonstrate interaction, not cartography, and points to Figure 2 for the real boundaries. **No figure is a mock vulnerability score** — none exists yet, and inventing one for a funder document would misrepresent the project | Resolved |
| 2026-07-29 | Toolbox figure took 3 attempts | A worked example has to *vary* to be worth printing. Using the district layer as the overlay gave every division ~100% (districts tile the country); a 10 km coastal band was nearly as flat, Sri Lanka being narrow. A third attempt reported a maximum of 12 km inland — wrong, because simplifying each division independently left slivers, so the union's **interior holes were counted as coastline** | Union the buffered divisions and take **exterior rings only**. Distance now runs 0–107 km with 54 divisions within 5 km of the sea, and the interior gradient is visibly correct. Worth remembering: a figure that comes out uniform is usually a bug, not a finding | Resolved |
| 2026-07-29 | D5 SRS | Needed a specification credible to a funder, not a wish-list | `SRS.md` → `SRS.docx` (30 pp). Written for **GGGI review**: methodology and data provenance up front, 53 functional and 9 non-functional requirements, and a **§10 Delivery status** table stating plainly what is built versus specified — the database and data foundation are real, the application layer is not. Annex A is a **17-screen walkthrough of the prototype**, each figure tied to the requirements it illustrates and captioned as *prototype, not delivered software*. Annex C lists the passing negative tests, which is what shows the integrity rules are enforced rather than merely described. Every quantitative claim (174 / 330 / 243 / 3,664 / 1,783 / 65,976.7 km² / 17,826) was machine-checked against the source artefacts — 0 mismatches | Resolved |
| 2026-07-29 | Word packaging | Pandoc renders most tables correctly but silently mangles others: short tables get **no `tblGrid` at all** (later columns fall off the page), and tables it does size are sized from content length alone, making a "Method" column so narrow that POST wrapped one letter per line. Separator-dash width has no effect — the behaviour depends on whether content forces wrapping | `build_docx.py` post-processes the `.docx`: computes column widths from the source markdown, then rewrites **every** table's `tblGrid` *and* its per-cell `tcW` (which override the grid in Word), with a 1250-DXA floor. All 26 tables now sized, none below the floor, each summing to the text width. Verified by rendering to PDF and reading the pages | Resolved |
| 2026-07-29 | Screenshot capture | Headless Chromium would not start in the sandbox (missing X libraries, no root) and the browser extension mangles `file://` URLs | `apt-get download` + `dpkg -x` into a user-writable prefix with `LD_LIBRARY_PATH` — 16 libraries, no root required. Added `fonts-noto-color-emoji` after the first pass rendered sidebar icons as tofu boxes. 17 screens captured at 2x against the real catalogue | Resolved |
| 2026-07-29 | D1 + D2 diagrams | Phase 0's two diagrams were still outstanding, and the Figma connector was never authorised | Generated with Graphviz instead — `design/architecture/generate_diagrams.py`, same reproducible pattern as the ERD. **D1** shows 6 layers (users · inputs · frontend · FastAPI · PostgreSQL · agent) with **border style carrying status**: solid = built and verified, dashed = designed only. So the picture states plainly that the database is real and everything above it is not yet. **D2** traces raw value → `import_batch` → normalisation → weighting → hazard/exposure indices → `vulnerability_result` → SSP projection → monitoring, with the A3 period rules and the `v_profile_readiness` gate drawn as constraints rather than left implicit. Both document the system **as built**, not as originally imagined | Resolved |
| 2026-07-26 | ERD regenerated | `erd.dot` was **hand-written**, which is exactly why it silently went stale when D8 added five tables — nothing tied the picture to the schema | Replaced with `generate_erd.py`, which **parses the DDL** (all three SQL files, including `ALTER … ADD COLUMN`) and emits `.dot/.svg/.png`. Now **28 tables, 65 relationships, 0 unclassified**, grouped into 9 colour-coded modules with the D8 toolbox and `import_batch` as their own clusters. Also switched `rankdir` LR→TB: LR produced a 1499×4160 strip (ratio 0.36) that was unusable on a page; TB gives 3141×2031 (~1.55, landscape). Re-run after any schema change — the diagram can no longer disagree with the database | Resolved |
| 2026-07-26 | SQL verification | `pglast` proves syntax only — it accepted an INSERT naming a column that did not exist | Added a semantic pass over all three DDL files: builds the table→column map (including `ALTER … ADD COLUMN`) and checks every INSERT column list and every FK target. Caught `spatial_layer.output_note`; now **0 problems across 28 tables / 11 types**. Worth re-running after any schema edit | Resolved |
| 2026-07-26 | UI prototype refresh | Prototype still carried invented DS lists (21 for Central) and 3 hand-written sample profiles | Regenerated its reference data from the real artefacts: **330 DS divisions**, 174 canonical variables, **243 real profiles** with carried-forward weights. Rewrote the data-officer wizard to the upload→confirm→save order and added the Profile weights screen. Verified headlessly under jsdom: import→save→re-edit→v3 loop, all roles/screens, **72 weights-screen and 216 map combinations — 0 runtime errors** | Resolved |
| 2026-07-02 | D7 prep | Inventory of all 243 workbooks → `design/ingestion/variables_inventory.csv` (1,916 variable rows, 243 profiles, ~300 distinct variables, hazards: Flood/Drought/Landslide) | 3 anomalies for sector teams (see VARIABLES_INVENTORY_NOTES.md): NW Cattle–Flood exposure sums 85; Uva Hu.Settlements–Landslide sums 105; Eastern Paddy–Flood duplicate E7. Livestock subsector names need normalization (Buffalo/Buffaloa/Pig Sheep) | Mostly resolved 2026-07-26 — duplicate E7 fixed by unique catalog codes; subsector names normalised in generator (`LEGACY2NEW`). The two weight-sum anomalies are moot: weights are being re-confirmed per profile anyway (see Weights row). Inventory retained as the source of province coverage + legacy weights |
| 2026-07-29 | B1 first real run | `check_prereqs.ps1` reported PostgreSQL missing, so a fresh PostgreSQL 16 install was attempted via winget — it failed with "port not available." Investigation found a **pre-existing PostgreSQL 15 server already installed and running** (2026-05-03, `C:\Program Files\PostgreSQL\15`) with a full PostGIS bundle (PostGIS, pgRouting, MobilityDB, h3-pg, pgpointcloud, ogrfdw) — from an earlier, unrelated setup on this machine | No install needed. Built `riskradar` as a new database on the existing server instead (does not touch any other database there). The failed PG16 winget attempt left no files behind — nothing to clean up. Credentials for the existing server: `postgres` / (given by Milinda in chat, not stored here) | Resolved |
| 2026-07-29 | `apply_native.ps1` crash on optional pgvector check | Script sets `$ErrorActionPreference = 'Stop'`; the pgvector probe (`CREATE EXTENSION IF NOT EXISTS vector` with stderr redirected via `*>$null`) is *meant* to fail silently when pgvector is absent, but the redirected native-command stderr became a terminating error under `Stop` and aborted the whole build | Wrapped that one call in `try {...} catch {}` so the expected failure is swallowed and `$HasVector` still resolves correctly from `$LASTEXITCODE` | Resolved |
| 2026-07-29 | `generate_seed.py` emitted an invalid enum value | Line ~231 wrote `direction = "lower_is_worse"` for indicators with a `-` relationship (e.g. `PCT_FOREST_COVER` — more forest cover lowers vulnerability), but `schema.sql`'s `indicator_direction` enum only defines `higher_is_worse` / `higher_is_better` — no `lower_*` variants exist. Seed load failed 4,432 lines in with `invalid input value for enum indicator_direction`. Same class of bug as the 2026-07-26 "Seed SQL was broken" row: parsing had never caught it because nothing resolves enum labels against the type definition | `lower_is_worse` ⇔ `higher_is_better` are the same semantic (low value is bad ⟺ high value is good), so fixed the generator to emit the enum's actual label and regenerated `seed_all.sql` (counts unchanged: 174/243/3,664, 99 rows affected). Per working rule 1, the generator was fixed, not the generated SQL | Resolved |
| 2026-07-29 | `load_spatial.ps1` — GDAL build has no live PostgreSQL driver | The `ogr2ogr` bundled with the PostGIS install (GDAL 3.7.1) was built **without libpq** — `ogr2ogr -f PostgreSQL ...` (direct-connect driver) failed with "Unable to find driver \`PostgreSQL'." Only the `PGDUMP` driver is present. No QGIS/OSGeo4W install exists on this machine to fall back to. This path had never been exercised end-to-end before (see the "Nothing in backend/ has been executed against a real server" note) | Switched all three shapefile loads (`SL_RDSD`, `SL_PD`, `SL_DSD`) to `ogr2ogr -f PGDUMP` → temp `.sql` → `psql -f`, using the same GDAL binary already present — no new install required. Refactored into a shared `Load-Shapefile` helper | Resolved |
| 2026-07-29 | `load_spatial.ps1` — GDAL LAUNDERs field names | The PGDUMP/PG writer lowercases shapefile field names by default (`ADM3_EN` → `adm3_en`, same for `ADM1_EN`/`ADM2_EN`). The merge SQL referenced the original-case quoted names (`s."ADM3_EN"`), which would have failed on the very first real run of this script regardless of which ogr2ogr driver was used — a latent bug, not something the driver switch introduced | Updated all references (the DSD↔register join, the "no match" check, and the province/district `spatial_feature` inserts) to the lowercase laundered names | Resolved |
| 2026-07-29 | `load_spatial.ps1` — PowerShell mangled SQL with embedded double quotes | The `Sql()` helper passed multi-line SQL to `psql -c $string`. When PowerShell serialises a string argument containing embedded `"` (JSON literals like `'{"name":{"type":"string"...}}'`, or quoted identifiers) into a native process's raw command line, it can drop the inner quotes — `psql` received `{name:{type:string...}}` and errored with `invalid input syntax for type json`. Not caught earlier because most `Sql()` calls in this file happened not to contain literal double quotes | Rewrote `Sql()` to write the statement to a temp `.sql` file and invoke `psql -f`, sidestepping native-argument quoting entirely. Also converted the one remaining raw `psql -c` heredoc call in the file to use the same helper for consistency | Resolved |
| 2026-07-29 | `smoke_test.sql` — geometry-type test tripped the wrong trigger | The "polygon rejected on the LINESTRING-only ROAD layer" test inserted a polygon **without** the `class` attribute that the ROAD layer's `attribute_schema` also requires. PostgreSQL fires multiple `BEFORE INSERT` triggers in alphabetical order by trigger name, so `spatial_feature_attr_guard_trg` ran before `spatial_feature_type_guard_trg` and raised "Layer 3 requires attribute \"class\"" instead of the intended geometry-type error — an ordering detail nothing should rely on, and the test wasn't isolating the one condition it meant to check (the LULC attribute test right below it already did this correctly, with valid geometry) | Added a valid `'{"class": "A"}'` attributes payload to the ROAD-layer test insert so only the geometry-type mismatch is exercised, matching the isolation pattern already used by the attribute test. Shared by both `smoke_test_native.ps1` and `smoke_test.sh` (Docker), since both run the same `smoke_test.sql` | Resolved |
| 2026-07-29 | **B1 database — first successful build** | First real execution of the native-PostgreSQL route end-to-end, against the pre-existing PostgreSQL 15 + PostGIS server (see row above) | `apply_native.ps1 -Reset` then `smoke_test_native.ps1` — **all smoke tests pass.** 9 provinces, 3 hazards, 8 sectors, 12 subsectors, 174 variables, 269 aliases, 243 profiles, 3,664 memberships (1,881 weighted / 1,783 awaiting weight, as designed), 330 DS divisions (all valid geometry, no overlaps, total area 65,976.7 km² — confirms EPSG:5235 was used), 6 spatial layers, 6 toolbox operations. `v_profile_readiness`: 0/243 computable pending weights, exactly as intended (B3 must refuse until weights are filled in-app) | **Resolved** |
| 2026-07-30 | **Reviewed the deployed Risk Radar** | Milinda: *"i need improved version of it"* — a first version is already live at `riskradar.geoinfobox.com` with a Django REST API. Everything designed so far had been designed **without looking at what users already have**, which is how a replacement ends up being an unfamiliar rewrite rather than a better version of the same thing | Inspected the running system first-hand, **read-only**, stopping at the point each issue was confirmed. Findings in `design/LIVE_SYSTEM_REVIEW.md`, UI mapping in `design/ui/UI_REDESIGN_BRIEF.md`. Two classes: **functional** (a score cannot be decomposed; no time, track or provenance; DSD layer ≈20 s to draw because geometry ships as raw GeoJSON; stale session state shows *Logout* while the API returns 401) and **security** (DRF has no `DEFAULT_PERMISSION_CLASSES`, so every endpoint including writes is open to anonymous users; `DEBUG=True` dumps settings, paths and the full URL table; running on `manage.py runserver`; CORS allow-all with credentials; cookies not `Secure`). **I did not log in** — entering a password into a form is a hard limit — so *Add Data* and *My Data* remain uninspected. Recommended rotating the shared credential, which is weak and has passed through a chat log | Resolved |
| 2026-07-30 | **0% and "no data" are the same colour** | The single most consequential finding. At DSD level most divisions render `0.0%` and are left uncoloured; the three-band legend starts at 1%, so zero falls outside every band. Colombo, Galle, Kalutara, Badulla, Kilinochchi, Mannar and Mullaitivu all read `0.0%` at district level — which cannot be a real assessment result. **A planner reading that map concludes those areas are safe.** For a tool meant to steer adaptation finance that is not a presentation nit, it is a correctness failure | Promoted to a first-class concept rather than a UI fix: **assessed / pending / unassessed** defined in SRS §1.3, required to stay distinct through database, API and map (FR-5.8 to FR-5.11), given a system-wide non-functional rule (NFR-10 *honest absence* — absence must not become zero at a layer boundary, which is where it usually does), and surfaced as a coverage statement on every view. `v_profile_readiness` already distinguishes *pending*; what was missing was carrying it to the surface | Resolved |
| 2026-07-30 | SRS revised to v1.1 | The SRS was written before the deployed system was seen. Left as-is it would have specified a replacement without acknowledging the thing being replaced — and would have repeated its geometry-delivery mistake, since NFR-3 said only "2 seconds per province" with nothing about how geometry reaches the browser | Added **§1.5** (what the deployed system does well and this keeps — the map-is-the-app structure, the sector/hazard/level controls, four admin levels, public read, print — against what it cannot do), **§5.5** (its `vulndata` row is a *conclusion*, not an indicator, so FR-11.2 migrates it as an archived result with composition recorded as unknown rather than faking provenance), **§6.11** (8 migration requirements), **Annex A.0** (screen-by-screen continuity table), **Annex D** (the review). Rewrote **§6.5** into five groups: core map, explaining the score, coverage, time, interface states. Hardened NFR-3 to require vector tiles, and added NFR-4b/4c/4d so the deployed system's security posture is not rebuilt by default. **Nothing in §2, §5.1–5.4 or §8 changed** — the review confirmed the model rather than challenging it; every capability the deployed system lacks is one the schema already provides for. 91 requirement IDs, no duplicates, no dangling cross-references (checked programmatically). Opened **O-7** (do the extra 7 sectors / 17 hazards stay?), **O-8** (is GND data real?) and **O-9** (deployed security findings unremediated — independent of this build, should not wait for it) | Resolved |
| 2026-07-31 | **Reviewed the existing React frontend** | Milinda added `FrontEnd/` to the repo. Everything designed so far had assumed the frontend did not exist | 45 files, ~8,400 lines. **The map-select → side-panel entry flow already exists and already supports multi-select** (`FindByName_DSD.jsx` → `DataInput_SidePanel_v4.jsx` takes a *list* of divisions) — so the workflow agreed the previous day is half-built, not new. **What is entered is a 1–10 ordinal from a dropdown**, multiplied by 10 and displayed with a `%` sign — so the live map's "66.0%" is an average of ten-point opinion ratings. Located the 0%-vs-no-data bug exactly: `DataAnalyse_Map.js:70`, where 0, null and undefined all fall through to `transparent`. Two more in the same expression: a rating of exactly **10 renders invisible** (`< 10`), and the identical rule is written three times with one copy disagreeing. Also `response.ok` tested on an axios response (always undefined) so **every POST is followed by a redundant GET+PUT** — 45 requests to save 15 divisions. Four numbered copies of the side panel with three live; two mapping libraries (Leaflet not in `package.json`); `dist/` committed; hardcoded API URL; no tests. Full findings in `design/ui/FRONTEND_CODE_REVIEW.md` | Resolved |
| 2026-07-31 | Map-first data entry workflow | Milinda: *"i do not need something like this... i need to visually select ds division and add vulnerable data"* — rejecting both the schematic tile prototype and the workbook-first flow. Then clarified the order: sector → subsector → hazard → **weights** → import, with experts editing imported values and general users giving only a perceived rating | `design/ui/DATA_ENTRY_WORKFLOW.md`. Context resolves to one profile, then the map becomes the **worklist** — coloured by entry state (empty / part-filled / complete / error), not by vulnerability, because nothing is computed yet. Resolved the one conflict in the stated order: weights come before upload, but the workbook carries a WEIGHTS tab, so **the screen is authoritative and the file advisory** — on upload a diff is shown and the officer chooses. Two open questions raised: min–max normalisation shifts every score as the last divisions arrive (recommend holding until a province is complete), and part-filled divisions should be *pending*, not scored on a subset | Resolved |
| 2026-07-31 | **Merged an independent IEEE 29148 SRS** | Milinda commissioned a parallel SRS from Gemini 2.5 Pro — 20 modules, ~180 requirements, Angular/NestJS/GeoServer/Kubernetes stack, 5 AI agents. Written without sight of anything built, so taken wholesale it would discard the 174 variables, 243 profiles, the tested 28-table database and the running React frontend | Merged selectively — 6 modules adopted, 9 adapted, 3 deferred to a new Annex F, 2 rejected. Analysis in `SRS_INTEGRATION_GEMINI.md`. **Three conflicts settled by Milinda, all to the recommended option:** (1) risk equation — keep `Vulnerability = f(Exposure, Hazard)` and add `Risk = H × E × V` as a *second, separately stored output*, because our 174 variables are already split into two domains and re-classifying into three would invalidate all 243 profiles; the new asset-exposure term needs no new collection, it is a spatial-toolbox intersection; (2) stack — keep React + FastAPI + PostGIS, take **GeoServer only** for OGC; (3) scope — core plus cheap wins. **Three real gaps the draft caught: Tamil** (NFR-8 said English + Sinhala only — a compliance failure for a Sri Lankan government platform, now O-10), **no audit log** in any of the 28 tables, and **no OGC services**. Also adopted AHP + entropy weighting (AHP's consistency ratio is exactly the credibility check the offline panel sign-off lacks), 18 coded validation rules, time slider and swipe, health check and backup. Corrected four Sri Lanka facts the draft got wrong (331 vs 330 DSDs, 14,022 vs 14,019 GNDs, storage SRID, weights summing to 1.0 vs 100 per domain) | Resolved |
| 2026-07-31 | SRS v1.2 | v1.1 had no risk layer, no audit trail, no OGC, no Tamil, and every requirement read as equally mandatory | New **§1.6** (how the draft was merged), **§2.6** (the risk layer, and why two decompositions of exposure coexist without contradiction), **§6.12** risk assessment, **§6.13** audit and versioning, **§6.14** interoperability, **Annex E** (18 validation codes as an interface contract — addable, never renumbered), **Annex F** (Phase 2 backlog recorded with reasons, so deferral is visible rather than silent). Added FR-3.8–3.11 for AHP/entropy, NFR-11–13, a Must/Should/Could convention, and O-10/O-11. **§2.1–2.5, §5.1–5.4 and §8 untouched.** 122 requirement IDs, no duplicates, no dangling cross-references (checked programmatically); 47 tables sized in the docx build | Resolved |
| 2026-07-31 | **Independent review of SRS v1.2 (GLM 5.2)** | Milinda commissioned a third-party review. 24 recommendations across IEEE 29148 compliance, functional completeness, technical consistency and editorial | Adopted 20, adapted 2, declined 2 — all with reasons. Strongest catch was the **dual meaning of *exposure***: §2.1 uses it for *characteristics of the exposed system* (a vulnerability domain), §2.6 for *assets in harm's way* (a risk multiplier). v1.2 acknowledged the tension in prose but kept one word for both. New **§2.7** names them apart — *exposure characteristics* vs *asset exposure* — and FR-5.16 now forbids a bare "exposure" anywhere in the interface. The stored enum stays `exposure`, since 3,664 rows carry it; only labels change. Also adopted: acronyms list, carry-forward flag semantics, AHP justification text, community rate limiting, SSP parameter mechanism, monitoring entry path, raster deferral stated openly, AI graceful degradation, expert-review states for AI answers, notifications (§6.15), and NFR-14–18 | Resolved |
| 2026-07-31 | **Risk index was mathematically wrong — caught by testing the review, not by the review** | v1.2 defined `Risk = H × E × V` with all three terms in [0,1], then applied the same five-band classification used for vulnerability. Neither the Gemini draft nor the GLM review questioned it | Simulated 200,000 divisions with independent uniform inputs. The raw product has mean **0.125** and puts **78% of divisions in "very low" and 0% in "very high"** — not a finding about Sri Lanka, an artefact of multiplying fractions. Changed FR-12.1 to the **geometric mean** `(H·E·V)^(1/3)`, which preserves the properties that made the multiplicative form right (any zero term zeroes risk; no term compensates for another) while returning mean 0.422 and a usable spread across all five bands, on the same scale as vulnerability so the two can share a legend. Worth remembering: a formula that type-checks can still be unusable, and the cheapest way to find out is to run numbers through it | Resolved |
| 2026-07-31 | Declined two of the 24 review recommendations | R1 asked for a ±0.01 tolerance on the weight-sum rule, reasoning from floating-point drift. R19 asked to renumber FR IDs so they ascend down the page | **R1 rests on a false premise.** `profile_indicator.weight_pct` is `NUMERIC(6,3)` — exact decimal, not float. 33.333 + 33.333 + 33.334 is exactly 100.000, and three decimal places always admit an exact split. Loosening an exact rule to accommodate a problem the schema does not have would let a genuine 99.99 through. Instead FR-3.3 now states the exactness is achievable and required, clients must round to 3dp before submitting, and FR-3.3b makes the **UI** help the user reach 100 (distribute the remainder) rather than the rule bend. **R19 was declined** because a requirement ID is a stable name, not a position — renumbering would falsify every ID already cited in this log, in the UI briefs and in the review itself. Added an explicit identifier convention in §6 instead, and Annex G lists everything in numeric order | Resolved |
| 2026-07-31 | Annex G is generated, not written | IEEE 29148 wants requirements-to-verification traceability. A hand-maintained 146-row matrix drifts the first time a requirement is added | `generate_traceability.py` derives Annex G from the requirement tables in SRS.md itself and injects it between markers; `--check` fails a build if it is stale. Assigns verification method per section (Test / Demonstration / Inspection / Analysis) since that is a property of the kind of requirement, not the line. **First run reported itself permanently stale** — `collect()` was reading the generated annex's own table rows and doubling every requirement. Fixed by stripping the marker region before parsing. Now 146 requirements: 125 FR, 21 NFR, 120 Must / 24 Should / 2 Could | Resolved |
| 2026-08-01 | SRS v1.3 → expert panel | v1.2 had been reviewed by two AI systems but never by the people who own the domain. A specification that has only ever been checked for internal consistency can be consistently wrong | Issued `SRS_v1.3_Expert_Panel_Questions.docx` — the open modelling choices put as questions rather than buried as assumptions, each stated with the option already applied so a non-answer still leaves the build unblocked. Panel responses recorded in `SRS_v1.3_Panel_Review_Resolution.docx` (2026-08-06), and `Expert Review/Expert rewview_v1.docx` alongside | Resolved |
| 2026-08-06 | SRS v2.0 | v1.x had grown by accretion — three merges and two review rounds layered onto a document written before the deployed system was seen. It could no longer be handed to a developer and read straight through | Rewritten as **v2.0: complete and self-contained**, buildable without reference to any earlier document. Panel answers folded in as definite rules, each marked **[P-n]** and listed in **Annex C** with what would change if reversed — so the build proceeds on stated assumptions instead of stalling on open ones. §10 replaced the B/F numbering with **ten stages**, each naming what must be true before the next begins | Resolved |
| 2026-08-07 | **The formula changed again — third time in eight days** | v1.2 used the geometric mean `(H·E·V)^(1/3)`; v2.0 used `√(H × E)`, chosen because the raw product clusters near zero and leaves the map nearly uninformative. The panel instructed otherwise | **v2.1: the published index is the raw product `H × E`, min–max renormalised within its own province** (P-14). Renormalisation fixes the clustering that the square root was introduced to fix, so the spread is recovered — but at a price the square root did not carry, and it must be understood: the score is now **relative to its province**. No cross-province comparison, no national ranking, every province yields exactly one 1.000 and one 0.000 per profile, and **a division's published score moves when its neighbours' data arrives**. That last point is not yet resolved anywhere: publishing a partly-collected province shows scores that will change without anything about that division changing | **Resolved as specified — the moving-score consequence is open** |
| 2026-08-08 | **Frontend stack reversed: React → Angular** | Milinda: *"we use openlayers and angular for building this app."* OpenLayers was already correct in the SRS (§4.1, FR-5.1); the live change is Angular. This **reverses the 2026-07-31 Gemini-merge decision**, where Angular was proposed and rejected in favour of keeping the working React code | **Angular + TypeScript + OpenLayers**, backend unchanged. Recorded in SRS **v2.2 §4.1** with the reasoning stated in the spec rather than only here: a codebase the maintaining team cannot service is worth less than a rebuild, and the earlier decision rested on preserving working code — the grounds changed, so the decision follows. Cost is bounded because (a) Angular is a frontend choice and implies nothing for FastAPI, the 28-table schema, the generators or the toolbox, and (b) the **interaction** the React app arrived at — map-select → multi-select side panel — is the workflow in §6.2 and carries over; only its code does not. `FrontEnd/` marked **retired, reference only**: read it for the workflow and for the three defects a rebuild must not repeat (absent value rendered as zero, maximum rating invisible, redundant read-and-update after every write). Updated: `CLAUDE.md`, `PROJECT_GUIDE.md`, this file, `FRONTEND_CODE_REVIEW.md`, and `generate_diagrams.py` — script edited and **re-run**, per working rule 1 | **Resolved** |
| 2026-08-08 | Documentation had drifted for eight days | The tracker was last updated 2026-07-31 while the SRS moved v1.3 → v2.0 → v2.1. Seven files still specified **MapLibre**, which was never used — the deployed and local frontends are both OpenLayers. Nothing recorded the ten-stage plan, so two build plans were live at once. There is **no git repository**, so this file is the only history that exists | Reconciled all of it against v2.2 and added the rows above. `build_docx.py` was hardcoded to `SRS.md`; parameterised to take the source on the command line so a version bump no longer needs a script edit. Note `generate_traceability.py` targets the **superseded** `SRS.md` only — v2.x carries Annexes A–D and no Annex G, so it is not part of the v2.2 build. **Version control is still the outstanding recommendation**: three formula revisions in eight days with no history is the risk this log is carrying alone | Resolved — git still open |
| 2026-08-08 | **Git initialised** | Seven weeks of design decisions, three formula revisions and a stack reversal, with **no version history** — this file was the only record, and a bad edit to it was unrecoverable | `git init` + `.gitignore`, initial commit `37ff8e3`: **664 files, 42,296 insertions**, 78 MB. Policy is deliberately *not* clever — track source **and** the irreplaceable data (36 MB of shapefiles, the 243 distributed workbooks, the SRS builds actually sent for review); ignore only what a command can recreate (`node_modules`, `dist`, `__pycache__`, `.angular`) plus `.env` so the database password cannot be committed. `FrontEnd/dist` was in the tree and is now correctly excluded. `git fsck` clean, working tree clean | Resolved |
| 2026-08-08 | Git on a OneDrive mount — two traps | `git add -A` over the whole tree timed out mid-write and was killed, leaving a stale `.git/index.lock` and **464 orphaned `tmp_obj_*` files** in `.git/objects`. A later `git add` then refused to run at all, reporting another git process — the classic misleading message, since nothing was running | Removed the stale lock and the 464 temp objects, then staged in chunks rather than one pass: the 500 spreadsheets alone took 48 s. `git fsck` afterwards returns clean, so the repository is sound. **Two things follow.** (1) Run git **natively in PowerShell**, not through a mounted sandbox path — it is far faster and avoids this entirely. (2) `.git` living inside a synced OneDrive folder is a real corruption risk if two machines sync mid-write; exclude `.git` from sync, or push to a remote and treat that as the source of truth | Resolved — remote still recommended |
| 2026-08-08 | **Stage 5 started, out of order** | Milinda asked to start the frontend. Per the stage board, Stage 5 is gated on Stage 2 (catalogue + profile API), and **nothing above the database exists** — no FastAPI app at all. Building the map UI before there is anything to call is a real sequencing violation, not a formality: `FRONTEND_BUILD_GUIDE.md` §0 anticipates exactly this ("there are no scores yet, and there will not be for a while... it is the normal state, and it is what you build first") | Proceeded anyway, deliberately UI-only: every screen is a real, typed form calling the exact §9 endpoint shape, and every one of those calls fails today (502/ECONNREFUSED — no backend to reach) with the FR-5.20 error state shown, not hidden or mocked. `ApiClientService` now covers `/vulnerability`, `/vulnerability/{unit}`, `/coverage`, `/profiles/{scope}/weights` (GET+PUT), `/profiles/{scope}/weights/history`, `/imports`, `/imports/{id}/summary`, `/imports/{id}/rollback`. Nothing here is mocked data — R6 (never present a stale or empty view as data) held throughout | **In progress — see 5.1–5.8 above** |
| 2026-08-08 | Angular app named `frontend-angular/`, not `frontend/` | `FRONTEND_BUILD_GUIDE.md` §3 and `PROJECT_GUIDE.md` §2 both say `frontend/`. Windows/NTFS is case-insensitive, so `frontend/` cannot coexist with the already-present `FrontEnd/` (the retired React app) — `ng new frontend` would collide with it. This is a real constraint neither doc anticipated, not a stylistic choice | Used `frontend-angular/` to avoid the collision without touching `FrontEnd/` (kept exactly as retired reference, per CLAUDE.md). If `FrontEnd/` is ever deleted, renaming to `frontend/` is a one-line fix — nothing inside the app hardcodes its own folder name | Open — rename possible later |
| 2026-08-08 | No `/tiles/{level}/{z}/{x}/{y}.mvt` endpoint exists yet (5.2 not started) | FR-5.2/§4.3 require vector tiles — raw GeoJSON for 330 divisions is explicitly ruled out at production scale. But there is no backend to generate tiles from at all yet | Stopgap only, clearly flagged as such in code (`BoundaryService`): the 330 DS-division boundaries were simplified (0.004° tolerance, 4dp rounding, matching the figure already used inline in `risk-radar-ui-prototype.html`) via a one-off script (pyshp + shapely) into `design/spatial/ds_divisions.simplified.geojson` (267 KB), shipped as a static asset and joined to results **client-side** — same join the real tile endpoint will do server-side. Must be replaced when 5.2 lands; it is not a substitute for FR-5.2 at national/GND scale | Open — replace at Stage 5.2 |
| 2026-08-08 | OpenLayers map rendered zero canvases on first load | Console: `"No map visible because the map container's width or height are 0."` The map is constructed in `ngAfterViewInit`, inside a flex layout (`app-shell` → `app-main` → `map-page` → `map-page__body` → `map-page__map`) whose percentage-height chain hadn't resolved yet at that exact tick, so OL read a 0×0 target and skipped its first render pass with nothing to trigger a retry | Added a `ResizeObserver` on the map's target element that calls `map.updateSize()` on every resize, not just once after construction — standard fix for OpenLayers inside a layout whose size isn't known synchronously. Verified in-browser: boundaries paint (thousands of non-transparent sampled pixels on the vector layer's canvas) and the click/select handler is unaffected | Resolved |
| 2026-08-09 | **Experts disagree about whether a variable belongs at all — and there was no way to say so** | Milinda: some experts hold that a parameter belongs in the calculation and others that it does not. The obvious answer, "give it zero weight", **is not available**: `schema.sql` declares `weight_pct CHECK (weight_pct > 0 AND weight_pct <= 100)`, so 0 is rejected outright, and rev 4 of the weights addendum then gave NULL the meaning *"belongs to the profile, weight not set yet"* — the state **1,783 of the 3,664 memberships** occupy and the state `v_profile_readiness` uses to refuse computation. Both candidate values were already taken, and neither means "an expert thinks this does not belong". Encoding dissent as either would have destroyed the readiness signal the import screen depends on | New addendum `design/database/schema_consensus_addendum.sql` (working rule 4 — addendum, not an edit to `schema.sql`). **`profile_indicator.consensus`** ENUM `proposed / agreed / contested / rejected`, plus `consensus_note`, `dissent_count`, `decided_at`, `decided_by`. Only `agreed` and `contested` count toward a domain's 100; `rejected` carries no weight (CHECK) and no longer blocks readiness, so marking a variable rejected cannot make its profile permanently uncomputable. `contested` **must** carry a note (CHECK) — it is a claim about people. **Unanimity was deliberately rejected as the gate**: it hands every panelist a veto and would thin profiles to whatever nobody objects to. A contested variable is included *and* its dissent published. `v_profile_readiness` redefined accordingly; existing rows default to `agreed`, so applying this changes no published score | Resolved — 1.12 applies it to the live DB |
| 2026-08-09 | Official vs exploratory dashboard — resisted building a second data model for it | The second half of the same request: an official dashboard computed from agreed parameters, and a user dashboard for experimenting with different parameter sets and weights, so the two can be compared. The temptation is a parallel set of tables. It is unnecessary — `vulnerability_profile` already has `version` + `is_active`, its unique key already includes `version`, and `vulnerability_result` already stores `profile_id` as lineage plus a `method` JSONB weights snapshot. Official and exploratory are two **states of one object** | Same addendum: **`vulnerability_profile.publication`** ENUM `published / draft / sandbox`, plus `owner_user_id` (required for anything not published) and `derived_from_id` (so "what did you change relative to official?" is answerable without diffing membership sets by hand). A **partial unique index** enforces at most one `published` + `is_active` version per province × sector × subsector × hazard — preceded by a pre-flight `DO` block that names the offending profiles, because the pre-existing unique key includes `version` and so has never prevented two active versions of one scope. `v_official_profile` is what public surfaces read, so a draft cannot reach one by omission. A **trigger rejects any `vulnerability_result` written against a sandbox profile**: a preview is a pure re-weighting of values that are already stored and already normalised (normalisation is national with fixed bounds, P-2/P-3, so it does not depend on weights), which means `POST /compute/preview` needs no persistence at all — and permitting it would create scores with no reviewed lineage that an export could not tell from official ones | Resolved |
| 2026-08-09 | Sandbox aggregates are **not** public perception — and the comparison needs no new storage | The stated goal for the exploratory dashboard was to see how close expert judgement sits to public perception. An aggregate of sandbox runs cannot answer that: it is the opinion of whoever happened to click — self-selected, not population-weighted, and repeatable by one motivated person. Publishing it as "public perception" invites a sampling question with no good answer | Kept the two apart. The honest instrument is the **community track**, which already exists: `source_type` is `('data','expert','community')` and `vulnerability_result` is keyed by `source`, so expert, community and data scores for the same division and profile are already rows side by side. Added `v_track_divergence` — a view, not a feature — exposing all three plus the expert−community gap. **Both operands are renormalised within their own province (P-14), so the gap is meaningful within a province and must not be compared across one.** The sandbox stays what it is: a what-if tool. Recommended framing for it is **sensitivity**, not play — sweep a contested variable's weight 0→max and show how far the map moves; most of these disputes will prove immaterial, and that converts a governance deadlock into a measurement | Resolved — sensitivity view not yet built |
| 2026-08-09 | The addendum had two real defects that reading it did not catch | (1) `CREATE OR REPLACE VIEW v_profile_readiness` — replacement may only *append* columns, and the new definition puts `publication` fourth and the consensus counts in the middle, so it would have failed with "cannot change name of view column". (2) `ADD COLUMN panel_note` on `vulnerability_profile` — **already added by the weights addendum** in July. `pglast` parsed both without complaint, which is exactly the 2026-07-26 trap repeating: parsing is not verification | Fixed: `DROP VIEW IF EXISTS` first, **without CASCADE** so a dependency stops the run rather than being dropped silently; `panel_note` dropped from the addendum with a comment saying where it already lives and why. More usefully, the check is now **a kept script rather than a one-off** — `design/database/check_ddl.py` parses `schema.sql` + all addenda in order, builds a symbol table of tables, columns, enum types and labels, and resolves the new file's dependencies against it, including asserting that new columns and types do **not** already exist. It found defect (2) unprompted. Requires `pglast`. Run it after every schema change, before the ERD | Resolved — the checker is the durable part |
| 2026-08-09 | ERD regenerated; two generator gaps found | `generate_erd.py` has a hardcoded `FILES` list, so a new addendum is invisible to the diagram until the script is edited (working rule 1). Separately, the node renderer shows only keys and an `ALWAYS_SHOW` allowlist, collapsing everything else into "+ N more" — so `consensus` and `publication` would have been added to the schema and then not appeared in the diagram documenting it | Registered `schema_consensus_addendum.sql` in `FILES` and added both columns to `ALWAYS_SHOW`, with a comment saying why they earn the space: `consensus` is *whether* a variable belongs, `weight_pct` is *how much* it counts, and a diagram that shows the second but not the first misrepresents the model. Re-ran the generator: **28 tables, 66 relationships, 0 unclassified**; title updated to name all three addenda. Table count is unchanged because this addendum adds columns, not tables. Noted in passing and **not** changed: the title still reads "PostgreSQL 16" while the database actually runs on the local **PostgreSQL 15** server (see the 2026-07-29 rows) | Resolved — PG15/16 label drift open |
| 2026-08-09 | The 2026-08-09 decisions existed only as a conversation and a DDL file | The consensus column and the publication states are in the schema, but nothing above the database knows about them, and the reasoning — why zero weight is not exclusion, why the sandbox must not persist, why sandbox aggregates are not public perception — lived only in chat. A future session reading `schema_consensus_addendum.sql` alone would get the *what* and not the *why*, and would likely rebuild the exploratory dashboard as a parallel set of tables | Wrote `HANDOFF_2026-08-09_consensus_and_dashboards.md` at the repo root: nine tasks (T1 apply + negative tests · T2 scaffold FastAPI · T3 profile API with consensus · T4 weights-editor consensus UI · T5 stateless `POST /compute/preview` · T6 official vs exploratory surfaces · T7 sensitivity sweep · T8 track divergence · T9 import data-quality dashboard), each with its gate named, plus the ten rules that must not be broken and the traps worth not rediscovering. **T2 is the real gate** — FastAPI still does not exist, and Stage 5 was already built ahead of its gate once. Deliberately a work order, not a specification: SRS v2.3 wins over it wherever they differ, and the file is to be **deleted** when the tasks land | Open — nine tasks outstanding |
| 2026-08-09 | **O-9 answered — the project's single biggest blocker is closed** | Milinda: for the provinces that historically supplied only a composite hazard index, the component values *"will be provided later, we will gradually update the Excel files."* The question was never *when* but *whether*: "later" and "never" needed different engines. "Later" is the simpler outcome | **[P-5] confirmed, not merely assumed: components, one hazard construct.** No rule is needed for mixing two definitions of hazard in one map, and the composite index is a stopgap rather than a parallel definition. Three consequences. (1) The five provinces holding only the composite stay **pending** — no score, *not* a score of zero — until their components arrive; that is the existing coverage state (§5.6), so this needs no new machinery and Stage 5 can render it today. (2) The **558 blank hazard weights close as files arrive**, by collection rather than by decision — 31% of the 1,783-item backlog, and it needs no panel meeting. (3) **Stage 4 is no longer gated on an open question**, though it cannot produce hazard scores for those provinces until the data lands. Update SRS §2.8 and Annex C to mark P-5 confirmed | Resolved |
| 2026-08-09 | The same answer hardens the normalisation rule, and moves the blocker to O-8 | "Gradually update the Excel files" is direct evidence that data arrives in instalments over months. §2.2 permits exactly two behaviours under partial entry — **fixed bounds** or **hold until all 330 divisions report** — and holding is now clearly untenable: it would mean nothing publishes until national collection completes, for every one of the 134 count and extent variables | **[P-3] is no longer a preference, it is the only workable option.** Fixed bounds must be stated for the 134 variables that are not already percentages (36) or fixed-scale indices (4). This makes **O-8 the next real blocker**, ahead of O-1 and O-10. Three routes were put to Milinda: panel states a ceiling per variable · freeze the highest nationally observed 2020–25 value and use it permanently · let those variables hold. **The middle route is recommended** — defensible, explainable, and it moves. Awaiting his answer. Note this also confirms the 2026-08-09 rejection of "normalise over whichever divisions have arrived": under gradual collection that would move every published score on every upload | Open — O-8 |
| 2026-08-09 | **O-8 answered — and it reverses P-2** | Milinda, asked how the 0–1 scale should be set for the 134 count/extent variables: *"wait till all the variables associated with the province, and calculate the normalized values."* Chosen over freezing the observed national maximum and over having the panel state 134 ceilings, with the cost stated in advance and accepted | **Normalisation bounds become provincial, and "hold" becomes per-province rather than per-nation.** A province normalises once all of its own divisions have a value; it no longer waits for the other eight. This **reverses [P-2]** (bounds across all 330 divisions) and relaxes §2.2's hold rule, which currently reads "until all 330 divisions have a value". **What it buys:** stability — once a province is complete its bounds stop moving, which was the fatal flaw in the rejected "normalise over whatever has arrived" variant — and it needs no decision from anyone, which matters given the panel's pace on O-1. **What it costs, accepted knowingly:** (1) indicator values are no longer comparable between provinces, so the score-composition panel (FR-5.9) must be relabelled — a normalised rainfall of 0.8 now means "high for this province", not "high"; (2) per Annex C, the national colour scale must be withdrawn and any cross-province statement labelled indicative; (3) **the whole-Sri-Lanka national view floated on 2026-08-09 is no longer computable** — there is no common scale left to build it on | Resolved — SRS §2.2 and Annex C need updating |
| 2026-08-09 | Provincial bounds make the relativisation happen **twice** — flagged, not yet decided | With variables min–max scaled inside the province *and* the index min–max rescaled inside the province (P-14), a division's published score is province-relative at two separate stages. The second stretch was introduced for a specific reason: the raw product `H × E` clusters near zero and left the map uninformative. With provincial variable bounds, `H` and `E` are already stretched across the province's full range, so the clustering the second step exists to fix is **substantially pre-solved**. The second stretch still forces exactly one 1.000 and one 0.000 per province per profile, which is an artefact of construction, not a finding | **Not changed, deliberately** — P-14 is an expert-panel instruction dated 7 Aug 2026, two days old, and the formula has already moved three times in eight days (geometric mean → `√(H × E)` → raw product renormalised). Reversing it on my own reasoning would be the fourth. Recorded here so the question is asked once real values exist and the distribution can be looked at rather than argued about: **does step 2 still earn its place now that step 1 is provincial?** Note also that provinces differ in size — Northern has 22 divisions, Central 33 — so a small province's variables take more extreme normalised values under provincial bounds | Open — revisit at Stage 4.6 with real data |
| 2026-08-09 | **The national index needs the complete national dataset — and SRS v2.3 issued** | Milinda, on being told the whole-Sri-Lanka view was no longer computable under provincial bounds: *"but for calculating the national level vulnerability index you need all the values of the parameters."* Correct, and it reconciles what looked like a contradiction between his two answers. The national view is not cancelled; it is **a second phase with a precondition**, not a toggle on the map | Specified as **§2.2 "The national index requires the complete national dataset"**, with a phase table. **Phase 1:** a province completes → provincial bounds → its own scores publish, no cross-province comparison of any quantity. **Phase 2:** all 330 divisions hold a value → national bounds → national index and cross-province comparison become available for the first time. Three rules govern the transition and are new requirements **FR-4.3b, FR-4.5b, FR-5.24b**: phase 2 begins for all nine provinces at once, never per province; recomputing on national bounds **changes every already-published score** — the moving-bounds problem deferred rather than eliminated, but now moving **exactly once at a known, announceable moment**, which is precisely what makes it acceptable; and the switch is a **methodology version**, with phase-1 results retained and the change explained rather than allowed to look like a data correction | Resolved |
| 2026-08-09 | **SRS v2.3 built** — the spec had to move, not just the tracker | Three decisions in one day changed the measurement model, and the tracker is not the specification. Leaving §2.2 asserting national bounds while the build did the opposite is exactly the drift the 2026-08-08 documentation row was about | `SRS_v2.3.md` + `.docx` (`python build_docx.py SRS_v2.3.md`, 47 tables width-set). Edited: **§2.1** scope table — both steps now provincial, with the double-relativisation noted; **§2.2** rewritten — provincial bounds, provincial hold, P-3 superseded, plus the new phase-1/phase-2 section; **§2.8** dependency discharged, P-5 confirmed; **§11.3** O-8 and O-9 struck through, **new O-11** (does the index rescale still earn its place now that normalisation is provincial too — to be answered from the FR-4.15 distribution report at stage 4, *not* from theory); **Annex C** P-2 marked reversed, P-3 superseded, P-5 confirmed, P-14 annotated; **FR-4.3, FR-4.5, FR-5.9, FR-5.24** amended and **FR-4.3b, FR-4.5b, FR-5.24b** added. A "what changed in 2.3" box sits under the reading instructions so nobody has to diff. Pointers updated in `CLAUDE.md`, `PROJECT_GUIDE.md`, this file, `FRONTEND_BUILD_GUIDE.md`, `FRONTEND_CODE_REVIEW.md` and the handoff — six files, because a version bump that only lands in one is how v1.x drifted | Resolved |
| 2026-08-09 | **O-1 largely dissolved — an unweighted variable is excluded, not weighted** | Milinda: *"if there is no weight it means either its value is not significant, in that case rest of the available weights should add up to 100, if not system should give a warning."* Two follow-ups settled: **warn and refuse to compute** when the remainder misses 100 (not silently rescale — the weights used would not be the weights anyone typed), and **do not auto-exclude in the hazard domain** | **[P-6] superseded** — equal weight would have altered every other weight in the domain; exclusion leaves them untouched. Written into SRS §2.4 with three rules. The one that matters most: **exclusion is a recorded decision, never an inference.** The app asks on save and stores the answer as `consensus = 'rejected'` with an author and a date — which is exactly what this morning's addendum built, before this rule existed. A blank otherwise cannot be told apart from an oversight, and no published score may rest on one. New **FR-4.9b**; FR-4.9 rewritten; V013 unchanged | Resolved |
| 2026-08-09 | **My recommendation was too crude — measuring it against the seed showed why** | I advised "never auto-exclude in the hazard domain" to protect P-5, and Milinda took it. Parsing all 3,664 memberships out of `seed_all.sql` showed that rule blocking **everything**: all 243 hazard groups contain a blank, so a blanket hold left **0 of 243** profiles computable. The blanks are not one situation but two, and they are near-opposites | Refined to key on *what* is blank, which is [P-5] applied precisely rather than bluntly. **115 profiles weight the components and leave the composite index blank** — excluding it is literally what §2.8 mandates ("the composite carries no weight in a profile"), so they compute. **128 weight the composite at 100 and leave the components blank** — excluding there would hand the whole hazard domain back to the composite index, so they hold. Result: **97 of 243 profiles computable immediately** (Central 29, Western 24, Southern 22, Sabaragamuwa 22), 128 held on hazard components, **30 exposure groups warn** because their weights do not reach 100. Those 30 are the only part of O-1 that now needs real expert time — down from 1,783 memberships. **Lesson worth keeping: the 401-of-486 figure in §5.4 is a domain-group count and reads as far more encouraging than it is, because every hazard group is in it.** Check a rule against the seed before recommending it | Resolved |
| 2026-08-09 | v2.3 amended in place rather than bumped to v2.4 | The exclusion rule landed roughly an hour after v2.3 was built, from the same day's discussion. Two versions dated 9 August, differing by one rule, would be worse than one | Folded into **v2.3** and listed as item 3 in its "what changed" box, so the amendment is not silent. Defensible only because **v2.3 had not been distributed to anyone** — had it gone to the panel or to GGGI, this would have required v2.4. Docx rebuilt (48 tables). Reference check: **179 requirements, none referenced but undefined**. Still outstanding: **Annex D's requirement counts are stale** — four FRs have been added today (FR-4.3b, FR-4.5b, FR-5.24b, FR-4.9b) and that table has not been regenerated | Resolved — Annex D counts open |
| 2026-08-09 | **Two indexes, not two phases — and the unique key had no room for the second** | Milinda: *"same DS in a province gets two indexes — one for province and one for national, because we have different normalized values."* This replaces the phased formulation written into v2.3 four hours earlier, where the national index *superseded* the provincial one and every published score was restated once. Coexistence is strictly better: **nothing is recomputed, nothing is restated**, and the moving-bounds problem does not arise at the boundary at all — a provincial index is fixed when its province completes and never moves again | SRS §2.2 rewritten as **"Two indexes, not two phases"** with a comparison table; new **FR-4.3c** makes the scope a stored attribute rather than a presentation choice, because an unlabelled score is a defect and the label therefore has to exist in the data. FR-4.3b and FR-5.24b rewritten. **Schema consequence found by inspection, not by running it:** `vulnerability_result`'s unique key is `(ds_division_id, profile_id, source, scenario_id, year_start, year_end)` — the second index collides with the first. Added `index_scope` ENUM `('provincial','national')` to the key, plus `bound_min` / `bound_max` so FR-5.26 can show what produced a score. `v_track_divergence` now groups by scope too, or it would difference a provincial score against a national one. Provincial stays the **headline** per P-14 | Resolved |
| 2026-08-09 | Exploratory workspace restricted to experts (Q5) | Open access would generate aggregates that look like public opinion but measure only whoever used the tool — self-selected, not population-weighted, repeatable by one person | **Expert and Administrator roles only.** New SRS **§3.2** states the restriction and why, and points at the community track as the actual instrument for public perception. Practical benefit: no rate limiting and no public-facing disclaimer needed. The containment rules stand regardless — nothing persisted, exports watermarked, no tiles, no OGC | Resolved |
| 2026-08-09 | Dissent tally dropped; the accepted set is declared by the data-entering user (Q6) | I had built `dissent_count` — how many panel members objected. Milinda's answer makes clear that assumes a meeting procedure that does not exist. **A count nobody actually records is worse than no count**, because it looks like evidence | Column removed from the addendum before it was ever applied, so no migration. The declaration is made by the **user entering the data**, captured by `decided_by` + `decided_at`, with disagreement worth keeping written as prose in `consensus_note`. SRS §2.4 amended. The second half of his answer — *"if he later needs to add more then there should be a method with data input mechanism"* — became **FR-1.4b**: an expert adding a variable must be given a route to that variable's values in the same flow, and told how many divisions need one, because a variable added without data now **holds the whole profile indefinitely** under the §2.2 completeness rule. Plus **FR-1.4c** for the forced re-weighting | Resolved |
| 2026-08-09 | **331 is official — and the missing division is in Ampara** (Q7) | Milinda confirms the official count is 331 against 330 in the shapefile. Under the rules agreed the same day this stopped being a tidy-up: §2.2 requires **every** division of a province to hold a value before any of that province's scores publish, and the national index requires every division in the country | Located it rather than leaving it open. Compared the register's per-district counts against the published per-district counts: they agree on **24 of 25 districts**, and the totals differ by exactly one — **Ampara district, Eastern province: register 19, published 20**. Two candidates, unconfirmed: the **Kalmunai split** (officially two divisions, Kalmunai Muslim and Kalmunai Tamil, held as a single "Kalmunai" in the register) or **Eragama**. **Impact: Eastern province cannot publish any score, and the national index cannot be computed for any profile, until this is reconciled.** Recorded as O-2, now blocking. `dsd_register.csv` and the shapefile both need the fix; the 330 in the smoke tests becomes 331 | **Resolved 2026-08-10 — the Kalmunai split; see the row at the end of this log** | 
| 2026-08-09 | O-3 closed — some sectors are genuinely provincial (Q7) | Vegetables & OFC and Inland Fishery hold legacy data for Central only, and it was unclear whether collection was incomplete or the sectors simply do not exist elsewhere | Milinda: *"some sectors only exist in some provinces, so we have to live with that."* **No further workbooks.** The 16 extra I had costed are not required and **243 stands.** Confirmation C4 in §1a resolves the same way | Resolved |
| 2026-08-09 | Handoff file rewritten and the old one **deleted**, not left beside it | `HANDOFF_2026-08-09_consensus_and_dashboards.md` had been patched six times through the day as answers arrived. It still read correctly but only as a diff — "CHANGED 2026-08-09", rules numbered 1, 1b, 3, 3b — which is exactly how a reader ends up following a superseded rule that is still on the page | Replaced by **`BUILD_BRIEF_2026-08-09.md`**, written as settled state rather than as changes: 14 numbered rules each with its reason, a one-page statement of the model so the implementer understands what they are building, T1–T9 with explicit gates, the traps, and the open items. **The old file was deleted** — two live work orders is the drift the 2026-08-08 documentation row was about, and keeping the superseded one "for reference" is how it starts. History is in this log, which is where it belongs | Resolved |
| 2026-08-09 | A forward-looking brief does not tell you what is now **wrong** | `BUILD_BRIEF_2026-08-09.md` says what to build next, which is the wrong shape for updating code that already exists. The Angular client was written against SRS v2.2 and has been correct-but-stale since the morning; nothing listed, per decision, which of its files that makes wrong | Added **`CHANGES_2026-08-09.md`** — nine changes C1–C9, each with before / after / why / spec reference / **the specific files to edit**, plus a *"what did not change"* section (the formula, P-14, the three coverage states, 243 profiles) because over-correction is as likely as under-correction, and a **grep sweep list** for stale code and comments. The two files are explicitly scoped against each other so this does not become two live plans: brief = forward, changes = backward. **One real defect found while writing it:** `VulnerabilityComposition.provinceMin` / `provinceMax` in `core/models/vulnerability.model.ts` are misnamed under the two-index model — for a national-scope index the bounds are not provincial. Renamed to `boundMin` / `boundMax` in the register as a required edit | Resolved |
| 2026-08-09 | **`frontend-angular/` swept against `CHANGES_2026-08-09.md` C1–C9** | The Angular client (started 2026-08-08, still UI-only, no backend to call) was written against SRS v2.2 and needed every applicable change from the register | **C1** — `VariableContribution.normalisedValue` documented as province-relative; `division-panel` states "normalised within X Province" beside every indicator value and names the index scope. **C2** — added `IndexScope` (`provincial`/`national`) to `VulnerabilityUnit`/`VulnerabilityQuery`; renamed `provinceMin`/`provinceMax` → `boundMin`/`boundMax` (the defect C9 flagged); filter bar gained a scope control, `national` disabled with a stated reason (blocked on C8); coverage legend now names its scope in the heading and never mixes the two. **C4/C5/C6** — `ProfileVariable` gained `consensus`/`consensusNote`/`decidedBy`/`decidedAt`/`isCompositeHazardIndex`; `SaveWeightsPayload` now carries a `WeightDecision` per row instead of a bare weight. Weights editor reworked: a blank is resolved by an explicit **Exclude** action (name required, no auth yet so it's free-text) or by a weight, never inferred; hazard-domain component rows never get an Exclude button and show "held pending data" instead (C5), only the composite-index row does; a domain total now sums only `agreed`/`contested` rows, so an excluded row contributes nothing; added **Reconsider** to reverse an exclude, since nothing here should be a one-way door. **C8** — reworded `BoundaryService`'s docstring off a bare "330"; the asset still physically carries 330 features (Ampara division still missing) but nothing reads that count except `features.length`. **C3** needed no code change (existing coverage states already cover a composite-only province as `pending`). **C7** does not apply to anything built — the exploratory workspace it restricts is a separate, not-yet-built screen, and `/weights`/`/import` remain data-officer/expert screens per the §3.1 role table, not the workspace. `ng build` and `ng test` both pass after every change | Resolved |
| 2026-08-09 | **Landing page, sign-in and registration — and the app had no authentication at all** | Milinda: *"when the user clicks the link there must be a landing page and login based on the user type, and if you want to register for data entry as data entry authorised agency, expert, or public user, then only data can be entered."* Much of this was already specified — FR-12.2 self-registration with administrator approval, FR-12.3 a pending-registrations dashboard, FR-7.5 rejecting unauthenticated submission. **What did not exist was any of it in code:** no landing page, no login screen, no auth calls on `ApiClientService`, and `app.routes.ts` mapping `path: ''` straight to the map | Three answers taken. **(1) The landing page does not gate the map** — one route from it reaches the map with no account, so §3.1's transparency requirement survives; it is a front door, not a gate. **(2) "Data entry authorised agency" is a role held by a person, not an organisation** — no organisation table, `app_user.organization` stays free text. Consequence accepted: the system cannot reliably answer "which agency supplied this figure?" nor reassign an officer's work when they leave; adding the entity later is additive. **(3) All three types self-register, an administrator approves.** New SRS **§3.5** plus **FR-12.7 – FR-12.10**; new `schema_auth_addendum.sql` with `account_status` ENUM, `requested_role_id` (what they asked to be, kept distinct from what was granted), approval and rejection records. **Flagged rather than silently accepted:** requiring approval for *public users* too means every community rating waits on an administrator, which will suppress the community track — the only honest instrument for public perception. The remedy, if uptake is poor, is to auto-activate `community` alone; nothing else changes, since a public user can only submit a rating | Resolved — community-approval cost noted |
| 2026-08-09 | **Provincial scope was specified, described, and impossible to enforce** | §3.2 has said since v1 that a data officer or expert is bound to one province and that the bound is enforced server-side on every write. Found while writing the auth addendum: **the schema has no support for it whatsoever.** `app_user` carries no province and `user_role` is only `(user_id, role_id)`. Nothing in the database could tell you which province a user belonged to, so the rule could only ever have been enforced by convention in application code that does not exist yet | `app_user.province_id`, plus a **trigger on `user_role`** — a trigger and not a CHECK because the rule spans two tables. Granting `data_officer` or `expert` to a user with no province now fails with a message naming §3.2; granting `admin` to a user who *has* one also fails, since administrators are national. This is the kind of gap that would have been found in production by someone uploading another province's workbook successfully — V004 exists to catch exactly that and had nothing to compare against | Resolved |
| 2026-08-09 | Role seed contradicted the specification | `schema.sql` seeds `('admin','analyst','expert','community')`. The SRS role model has **never** contained an Analyst — it has a **Data officer**, which is what Milinda calls the "data entry authorised agency". Nothing referenced `analyst`, so it had gone unnoticed since the schema was written | Renamed in the addendum to `data_officer`, display name **"Data Entry Agency"**, with the descriptions of `expert` and `community` filled in to match §3 and §3.5. Noted in the SRS as a seed defect, not a specification change, so nobody reverses it later. `analyst` must not be reintroduced — it is on the sweep list in `CHANGES_2026-08-09.md` | Resolved |
| 2026-08-09 | `check_ddl.py` asserted against the wrong addendum | Adding `schema_auth_addendum.sql` made the checker report six false failures — it still held the *consensus* addendum's new-column list while that addendum had become part of the symbol table, so every column it legitimately creates looked like a collision. The tool caught its own staleness, which is the good version of this failure | `TARGET` is now `ORDER[-1]` — the file under test is always the last, everything before it is what it resolves against — and the new-column list carries a comment saying it belongs to `TARGET` alone and must be **replaced**, not appended to, when a new addendum arrives. Also now checks that the trigger functions it defines do not collide with an existing one. Re-run: **OK, 24 tables, 15 enum types, 5 views.** ERD regenerated: 28 tables, **68** relationships (up 2), 0 unclassified | Resolved |
| 2026-08-09 | **T1 — `schema_consensus_addendum.sql` applied and proven, per `BUILD_BRIEF_2026-08-09.md`** | The addendum had been written and statically verified (1.10, 1.11) but never run against the live database, and none of its six constraints had a test proving it actually fails for the stated reason — Stage 1's exit criterion is "a passing negative test", not "the statement ran" | Applied cleanly against the local PostgreSQL 15 `riskradar` database (`psql -f schema_consensus_addendum.sql`, zero errors) — confirmed a no-op on existing data exactly as its header claims: all 3,664 memberships default to `agreed`, all 243 profiles to `published`, `v_profile_readiness` distribution unchanged at 0/243 computable (nobody has recorded an exclusion decision yet; the schema now merely allows it). Wrote `backend/db/consensus_test.sql` + `consensus_test_native.ps1`, matching the existing `smoke_test.sql` DO-block convention, covering all seven rows of T1's table plus the two "must also pass" checks: rejecting `POTABLE_WATER_DROUGHT_SOU_V1`'s blank composite hazard index (its one missing weight) flips `is_computable` false→true as predicted by C5's worked example, and the 242 untouched profiles' domain totals are bit-identical to what the pre-addendum view would have computed. The whole test file runs inside one transaction that **always rolls back**, since several tests must mutate real seeded rows (a second published version, a sandbox profile, a throwaway `app_user`) to provoke a failure, and none of that may be left in the 243-profile dataset — verified empty afterward (`vulnerability_result` 0 rows, `app_user` 0 rows, no `consensus <> 'agreed'` rows). **Found and fixed a real bug while writing the runner, not in the addendum**: `consensus_test_native.ps1`'s `Find-Psql` function — copied from `apply_native.ps1` / `smoke_test_native.ps1` — combines a wildcarded `-Path` (`C:\Program Files\PostgreSQL\*\bin`) with `-Filter psql.exe`, which silently returns **zero results** in this Windows PowerShell 5.1, even though the same `-Path` alone and the same `-Filter` against a literal resolved path each work fine independently. It went unnoticed until now because every prior run either had `psql` on PATH already or set `$env:PGBIN` to a literal path, both of which skip the broken branch. Fixed in `consensus_test_native.ps1` by enumerating directories first (`Get-ChildItem -Directory`, no `-Filter`) and testing for `psql.exe` inside each with `Join-Path`/`Test-Path`. **Not fixed in `apply_native.ps1` or `smoke_test_native.ps1`** — same latent bug, out of scope for T1; whoever next runs either without `psql` on PATH will hit it. `check_ddl.py` re-run after this addendum was already part of the symbol table (see the row above): **OK — every dependency resolves.** `generate_erd.py` re-run: same **28 tables, 68 relationships** as above. Installed Graphviz (`winget install Graphviz.Graphviz`) and `pglast`, neither of which was present in this session | Resolved — T1 complete, T1b next |
| 2026-08-09 | **`Find-Psql` was broken in three scripts, not two — and the worst one was `check_prereqs.ps1`** | T1 found that `Get-ChildItem -Path 'C:\Program Files\PostgreSQL\*\bin' -Filter psql.exe` silently returns nothing on this PowerShell version — a wildcarded `-Path` combined with `-Filter`. It fixed its own new file and, correctly by the letter of the instruction, left the others alone and logged them. Checking the claim showed the same pattern in **`apply_native.ps1`, `smoke_test_native.ps1` *and* `check_prereqs.ps1`** — the third was not in the report. Two things make it worse than it looked: the branch **only runs when `psql` is not already on PATH**, so it fails precisely when it is needed and is invisible otherwise (which is why the 2026-07-29 end-to-end run passed); and in `check_prereqs.ps1` it would print **"PostgreSQL is not installed"** on a machine that has it, sending someone to install a second copy | Fixed in all three with the resolved-directory form the T1 script had already proven — enumerate the version directories, then `Test-Path` the literal `psql.exe`. The in-file note in `consensus_test_native.ps1` saying the others were "not fixed" is now corrected rather than left to mislead. **Judgement recorded, because it cuts against the instruction:** "out of scope, log it" is right for a discretionary improvement and wrong for a broken script the owner depends on to build the database. The scope rule exists to stop unrequested rewrites, not to preserve known breakage. A one-expression fix to three copies of one function is not a rewrite | Resolved |
| 2026-08-09 | Prerequisites the tooling needs were undocumented | T1 had to install **Graphviz** (`winget install Graphviz.Graphviz`, for `generate_erd.py`) and **pglast** (`pip`, for `check_ddl.py`) mid-task. Both are genuinely required by working rules 1 and 2 after any schema change, and neither was mentioned anywhere or checked by `check_prereqs.ps1` | Added an optional **"Design-tooling"** section to `check_prereqs.ps1` reporting Graphviz, Python and pglast with their install commands. Optional rather than blocking — the database builds without them; only regenerating the ERD and verifying an addendum need them. Better found before a schema change than after one | Resolved |
| 2026-08-09 | **Database password was pasted into a chat session** | T1 was given the live `riskradar` password in conversation. It handled it correctly in the moment — held only in `$env:PGPASSWORD` for the duration, never written to a file — but the exposure is not the file system. It is now in the session transcript, and if it was set at an interactive PowerShell prompt, **PSReadLine writes every command line to `ConsoleHost_history.txt` by default**, in plain text, indefinitely | Recorded as a standing practice rather than a one-off cleanup. **Check `(Get-PSReadLineOption).HistorySavePath` and grep it for `PGPASSWORD`**; if present, the password should be treated as disclosed and rotated. The durable fix is a **`.pgpass` file** or a `PGPASSWORD` set from `.env` (already gitignored) rather than typed, so it never reaches a prompt, a transcript or a chat window. Note the scripts already avoid prompting for it in a way that would echo | Open — needs the history check |
| 2026-08-09 | **`apply_native.ps1 -Reset` would have silently discarded both new addenda** | T1 applied `schema_consensus_addendum.sql` to the live database directly with `psql -f`, which is correct for the task. But the rebuild script still applied only base schema → weights addendum → spatial-model → seed. **A `-Reset` would therefore have produced a database missing the consensus columns, the index-scope split, the publication states and the sandbox trigger** — and nothing would have said so, because every one of those is additive and the seed does not reference them. The next person to rebuild would have found the application broken against a database that looked freshly built | Both addenda added to the sequence, renumbered 1/7 … 7/7. **Placed after the seed on purpose**, with a comment saying why: the consensus addendum opens with a pre-flight check that no two active profile versions share a scope, and run before the seed that check passes trivially against an empty table — the opposite of its point. Run after, it validates the real 243. This is the general trap with addenda applied by hand during a task: **applying it and registering it are two different jobs**, and only the second survives a rebuild. Same reason `generate_erd.py` and `check_ddl.py` both carry explicit file lists | Resolved |
| 2026-08-09 | No `.env.example`, though `.gitignore` promised one | `.gitignore` carries a comment reading *"keep it in .env, which is ignored; commit .env.example with the keys and no values"* — and no such file existed, so nobody knew which keys were expected. This is why the password ended up being passed by hand | Added `backend/.env.example` with the five `PG*` keys and no values. It also documents the better Windows route: a **`%APPDATA%\postgresql\pgpass.conf`** entry, which `psql` reads automatically, so the password never enters the environment, the shell history or a transcript at all | Resolved |
| 2026-08-09 | **Applying an addendum by hand and registering it in `apply_native.ps1` are two different jobs — verified both, not just one** | T1 (above) proved `schema_consensus_addendum.sql` against the live `riskradar` database by running it directly with `psql -f`. That proves the SQL is correct; it proves nothing about whether a fresh `-CreateDb` build reproduces the same result, since `apply_native.ps1`'s own file sequence is a separate, hand-maintained list that can drift from what was actually applied by hand — exactly the class of gap `check_ddl.py` exists to catch for symbol resolution, just one layer up, for the build script itself | Found `apply_native.ps1` already carries both addenda as steps 5/7 and 6/7, with a comment explaining they deliberately run **after** the seed (the consensus addendum's pre-flight duplicate-scope check would pass trivially against an empty `vulnerability_profile`, which defeats its purpose). Rather than take that on faith, built a disposable scratch database (`CREATE DATABASE riskradar_verify_...`), ran `apply_native.ps1 -Database riskradar_verify_...` end to end — all 7 steps, including the real 330-division spatial load (`ogr2ogr` is on PATH for PowerShell, just not for the bash session used earlier in this project) — then ran `consensus_test_native.ps1 -Database riskradar_verify_...` against it: **same pass, same 13 checks**, byte-for-byte the same summary counts as the live-applied `riskradar` (243 profiles, 3,664 memberships, 1,881 weighted / 1,783 awaiting, 330 divisions, 65,976.7 km²). Scratch database dropped immediately after; `riskradar` itself untouched throughout (243 / 0 / 0 profiles / results / app_users, unchanged) | Resolved — registration confirmed working, not merely present |
| 2026-08-09 | **Two agents were editing this repository at the same time** | The T1 session reported the `Find-Psql` fix and the `apply_native.ps1` addendum registration as *"updated externally in parallel with our session"*, and reasonably declined to claim credit. There was no external party: **both were made from the design conversation while the code session was running.** Nothing was lost this time — the edits were to different files, or to the same file at different moments — but the pattern is unsafe. Two agents holding stale copies of one file will overwrite each other silently, and the loser is whichever writes second. The tracker is the highest-risk file, since both sessions are instructed to append to §3 | **Rule: do not run the design conversation and the code session against this folder at the same time.** Finish one, let it report, then start the other. Where an overlap is unavoidable, the second session must be told what the first changed — as a list of files, in the prompt. Noted because the failure mode is silent: a clobbered edit leaves valid-looking code and no error, and the tracker row explaining a decision is exactly the thing whose loss is least likely to be noticed | Resolved — standing rule |
| 2026-08-09 | Registered schema outran its tests, and a build implied coverage it did not have | Flagged by the T1 session, correctly: `apply_native.ps1` now applies `schema_auth_addendum.sql` at step 6/7, but T1b is still open, so **a fresh `-CreateDb` build silently picks up the auth schema with no negative-test coverage behind it.** The sequencing error is mine — the addendum was registered in the same sweep that fixed the *missing*-registration bug, which put registration ahead of testing. The right order is write → test → register | **Left registered, deliberately**, with the gap made loud instead of silent: step 6/7 now prints `has no negative-test coverage yet - see T1b`, marked TEMPORARY and to be removed when 1.13 is ticked. Removing the step instead would have recreated the exact bug fixed hours earlier — an addendum applied by hand and absent from the rebuild — and relied on someone remembering to put it back. The cold-build proof does carry real weight: it shows the file applies cleanly, in order, against a live seed. What it does not show is that the constraints **reject** what they should, and those are different claims | **Resolved by T1b, below** |
| 2026-08-09 | **T1b — `schema_auth_addendum.sql` applied and proven; the TEMPORARY warning retired** | The gap the row above flagged: registration existed, enforcement did not. §3.2 — *"a data officer or expert is bound to one province ... enforced server-side on every write, never only in the interface"* — has been in the specification since v1 and, until today, had **no schema support at all**: `app_user` carried no province, `user_role` was `(user_id, role_id)` only, so the rule could only ever have been a convention nobody could check | Applied `schema_auth_addendum.sql` directly against the live `riskradar` database (`psql -f`, zero errors): `analyst` → `data_officer` confirmed renamed (no `analyst` remains), `app_user` gained `status`/`requested_role_id`/`province_id`/`approved_by`/`approved_at`/`rejected_reason`/`registered_at`, defaulting new registrations to `pending` + `is_active=false` as designed. Wrote `backend/db/auth_test.sql` + `auth_test_native.ps1` **from §3.2's and §3.5's text, not from the trigger's own code**, per instruction — there was no prior enforced behaviour to test against, only the specification's intent. Fifteen checks: the two `account_status` CHECKs (`app_user_approval_recorded`, `app_user_rejection_explained`, including blank-vs-NULL for the rejection reason); the centerpiece — granting `data_officer` **or** `expert` to a user with `province_id IS NULL` fails, granting `admin` to a user who **has** a province also fails, all via `user_role_province_scope` — plus, so the negative tests aren't vacuously true, the mirror-image **positive** checks (`data_officer`/`expert` **with** a province succeeds, `admin` **without** one succeeds); a fresh registration defaults `pending`/inactive; approving one (status + approver + timestamp together, not staged across two statements — the first attempt at this test tripped the same CHECK it meant to satisfy, fixed before running) flips `is_active` via `app_user_is_active_sync`; the role reseed holds. Every test rolls back — verified `app_user`/`user_role` at 0 rows before and after, on both the live database and the scratch one. **Proved from a cold build, as instructed:** fresh scratch database, full `apply_native.ps1` run (all 7 steps, real 330-division spatial load), then both `consensus_test_native.ps1` **and** `auth_test_native.ps1` against it — identical results to the hand-applied database. Scratch database dropped; `riskradar` unchanged throughout (243 profiles / 0 results / 0 users, before and after). TEMPORARY warning at step 6/7 removed (`apply_native.ps1` lines 186–192) now that "applies" and "enforces" are the same claim again. `%APPDATA%\postgresql\pgpass.conf` created this session (the project's own recommended-but-unbuilt route from an earlier row) so neither shell needs `$env:PGPASSWORD` going forward | **Resolved — Stage 1.13 done** |
| 2026-08-10 | **O-2 answered — the missing division is the Kalmunai split, and the project's last blocker is decided** | 331 official against 330 in the register and shapefile, the deficit in Ampara district. Two candidates had stood unconfirmed since 9 August: the **Kalmunai split** or **Eragama**. Under the provincial-completeness rule this was not a tidy-up — Eastern province could not publish any score and no national index could be computed for any profile | Milinda: take the **Kalmunai split** — `EAS-008 Kalmunai` becomes **Kalmunai Muslim** and **Kalmunai Tamil** — with polygons to be drawn later. Ampara goes 19 → 20, the register goes 330 → **331**. Three consequential sub-decisions taken with it, each with its alternatives costed and rejected. **(1) Codes: retire `EAS-008`, never reuse it, issue two new codes.** The alternative — keeping `EAS-008` for one half — leaves the code quietly meaning a smaller area than it did, so any historic value filed against it becomes ambiguous *and still resolves*; a retired code fails loudly instead. Renumbering Eastern alphabetically was rejected outright: 11 codes shift and every workbook, seed row and test naming one moves with them. **(2) Geometry: register both rows now with no boundary**, shown as an explicit **boundary-pending** state. Splitting the existing Kalmunai polygon provisionally was rejected — the boundary would be invented, and both halves would then carry a fabricated area into `sl_area_km2()` and everything derived from it. Holding the register at 330 was rejected because the 330/331 mismatch then resurfaces in every count for months. **(3) Values: both start blank, awaiting collection.** Copying the parent's values would double-count every count and extent variable across the pair; apportioning needs a split ratio, and the honest ratio is area — which is precisely what does not exist yet (`sl_apportion_2()` is also unbuilt, Stage 1.6). **Blocking consequence found by reading the DDL, not by running anything: `ds_division.geom` is `geometry(MultiPolygon, 4326) **NOT NULL**`.** Decision (2) is therefore not a data edit — it needs an addendum making geometry nullable and adding the boundary-pending state, per working rule 4. Recorded as **T1c**, ahead of T2 in `BUILD_BRIEF_2026-08-09.md`, because it is Stage 1 schema work and the register/seed/template regeneration hangs off it. **What this does and does not unblock:** the arithmetic and the register become correct and O-2 stops being an open question, but **Eastern still publishes nothing** — two of its divisions now hold no values by design, so the province is further from completeness than before, not closer. That is the correct outcome and must not be read as a regression: 330 was never complete, it was incomplete and silent. **Provisional until confirmed against an official source** — the split is the likelier of two candidates, not a verified fact; it is the naming that is provisional, never the count, since 331 is confirmed | Resolved — decision taken, T1c implements it |
| 2026-08-10 | **T1c implemented — 331 is now the real division count, not just the decided one** | The row above records the decision; this is the build. `schema_boundary_pending_addendum.sql`: `ds_division.geom` NOT NULL dropped; `boundary_status` added as `GENERATED ALWAYS AS (CASE WHEN geom IS NULL THEN 'boundary_pending' ELSE 'surveyed' END) STORED` — derived, so no call site repeats the NULL check and risks the exact defect already found once in `FrontEnd/`; trigger `vulnerability_result_no_boundary_pending`, mirroring the sandbox trigger, blocks a score for either new division until it has a boundary. `sl_area_km2()`/`sl_length_km()` needed no change — verified NULL-safe by construction (PostGIS-strict), not assumed. `sl_apportion()` was found to have the same "absent becomes 0" defect one layer up (`COALESCE(SUM(...), 0)` over a `LEFT JOIN` that a NULL geom always fails) — flagged in the addendum's header for whoever builds Stage 6, not fixed now: nothing calls it yet, and fixing a function ahead of its own stage risks guessing wrong about what Stage 6 needs | Nine files, in dependency order. **`backend/db/boundary_pending_test.sql` + `.ps1`** — 8 checks: a division with no geometry inserts and reads `boundary_pending`; one with geometry reads `surveyed`; `area_km2` stays NULL under `boundary_pending` (never 0); the national total moves by exactly a control fixture's own area and nothing else (a rounding artefact from double-rounding two already-rounded totals before subtracting was caught and fixed in the test itself, not the schema); a score is rejected for a pending division and accepted for a surveyed one (so the negative case isn't vacuous); supplying a boundary later via UPDATE flips the status automatically. **`dsd_register.csv`** — `EAS-008` row replaced in place (keeps Ampara's district-ordering, which the file's physical row order alone provides — there is no sort step in `generate_templates.py`) by `EAS-045 Kalmunai Muslim` and `EAS-046 Kalmunai Tamil`; first attempt broke CSV parsing by putting an unescaped comma in the `source` field, caught by parsing it back with `csv.DictReader` before moving on. **`generate_seed.py`** re-run: confirmed a true no-op (`git status` shows zero diff on `seed_all.sql`) — it never reads the register at all, division rows are `load_spatial.ps1`'s job, not the seed's. **`generate_templates.py`** re-run (243 workbooks, ~2 min): exactly 24 Eastern workbooks changed `n_rows` 44→45, verified by diffing `MANIFEST.csv` against the prior commit (a first pass wrongly flagged all 243 as changed — a path-separator mismatch, backslash vs forward-slash, in the verification script itself, not the generator). **`load_spatial.ps1` and `.sh`** — found and fixed a real bug: the register-to-shapefile merge was an inner `JOIN` on name, so a register row with no matching polygon would be **silently dropped**, not loaded boundary-pending — the omission failure mode of exactly the trap the design already warns about ("don't add a register row without geometry"), approached from the other side. Changed to `LEFT JOIN` with `geom`/`area_km2` NULL when unmatched. **`smoke_test.sql`** — `330` replaced with three separate assertions (331 registered · surveyed · boundary_pending = 2) rather than one loosened number, per instruction. **A real defect the cold build caught, not reasoning**: the first version asserted 330 surveyed; running it against the fresh build showed **329** — the old undivided Kalmunai shapefile polygon matches no current register row (its name matches neither new division) and loads as an orphan, so only 329 of the 330 shapefile polygons actually find a home. Fixed before it ever reached the live database. **`check_ddl.py`**: `schema_boundary_pending_addendum.sql` appended to `ORDER`; `DEPS`/`NEW_COLS` replaced (not appended) per the script's own convention comment — `OK, 24 tables, 16 enum types, 7 views`. **`generate_erd.py`**: file registered, `boundary_status` added to `ALWAYS_SHOW` with a comment on why it earns the space; re-run: 28 tables (columns only, as expected), 68 relationships. **Proof, twice**: a scratch database from `-CreateDb` through all 8 `apply_native.ps1` steps (now including this addendum as step 7/8, which must run *before* the spatial load since that load needs `geom` nullable to insert a pending row) — smoke test, and all three negative-test suites (consensus, auth, boundary-pending) — all passing, then dropped. Then applied to the **live** `riskradar`: the addendum, then a re-run of the (now-fixed) `load_spatial.ps1`, which left `EAS-008`'s old row in place at 332 total (`ON CONFLICT` upserts, it does not delete a retired code) — explicitly `DELETE`d after confirming zero `indicator_value`/`vulnerability_result` rows referenced it, landing on the same 331/329/2 the scratch run predicted. All four suites re-run against the live database: pass. **`frontend-angular`**: `BoundaryService`'s docstring corrected — it previously told a future reader to *"read `features.length` if a count is ever needed,"* which T1c makes actively wrong (the asset stays at 330 on purpose; the register is 331); `MapPageComponent.divisionTotal` documented as a stopgap fallback, not the authoritative count, for whoever wires up the real catalogue endpoint later. `ng build` passes | Resolved — Stage 1.14 done, T2 next |
| 2026-08-11 | **SRS body text swept to 331 — and the sweep found that most "330"s were right** | The code session's documentation sweep correctly declined to bulk-revise the SRS body and flagged it here instead. It described roughly 15 remaining "330" mentions as stale. **They were not.** Checking each one: only 7 were wrong; 4 correctly describe the *shapefile*, which genuinely still holds 330 polygons; and 3 are historical statements of what `[P-2]` or a superseded version asserted, which must not move. A blind find-and-replace would have introduced 7 new errors while fixing 7 old ones — the same class of mistake as the 2026-08-09 "401 of 486" row, where a plausible number was believed without being checked against the source | Fixed the 7 real ones: §1.1 purpose, the glossary's unit-of-assessment definition, §5.1 geography, §6.6's toolbox verification note, §10.0's status table, the Annex counts table, and O-6's translation scope. Left the shapefile and historical references alone, deliberately. **The important edit is none of those** — it is §5.1's "Known discrepancy" box, which said *"all counts in this document are against the 330 that exist"* and was therefore instructing every reader to misread every other number in the document. Rewritten as a three-number key: **331** is the unit of assessment and the completeness denominator, **329** is what the map can draw today, **330** refers only to the shapefile as supplied — plus an explicit note that a 330 in a historical statement is not a live count. §5.1 now states the polygon count and the division count as **deliberately different numbers** until the two boundaries are drawn, because the next person to find them unequal will otherwise assume a bug and "fix" it. Also caught in passing: **the two Kalmunai divisions have no confirmed Sinhala or Tamil name either**, so they belong to O-6's translation scope as well — noted on that row. Docx rebuilt (`python build_docx.py SRS_v2.3.md`, 49 tables). **Amended v2.3 in place rather than cutting v2.4**, on the same reasoning as the 9 August amendment: nothing here changes a rule, it aligns the prose with decisions the document already records, and v2.3 has still not been distributed. **If v2.3 has gone to the panel or GGGI, this becomes v2.4 and must be reissued** | Resolved — subject to the distribution check |
| 2026-08-11 | **T2 — FastAPI scaffolded, the database-layer decision made and recorded** | Stage 2 had never produced a line of application code — everything above the database was a plan. T2's own gate is unusually consequential: whatever `backend/app/` is built on now is expensive to change once T3 onward starts writing real queries against it, so the choice needed recording, not just making | **Decision: asyncpg with raw SQL. No ORM, no Alembic.** Reasoning: the schema is already the source of truth and has been treated that way since Stage 1 — constraints, triggers and functions (`v_profile_readiness`, `save_profile_weights()`, `sl_area_km2()`, the three 2026-08-09/10 addenda) encode rules an ORM would either have to duplicate in Python (drifting the moment one side changes, the exact failure mode `check_ddl.py` exists to catch one layer down) or bypass (silently reintroducing the "parsing is not verification" class of bug at the application layer instead of the DDL layer). A migration tool has nothing to migrate *to* — the schema evolves as hand-written, checked, addenda per working rule 4, and Alembic's own migration history would become a second, competing source of truth for exactly the thing `check_ddl.py`/`generate_erd.py` already keep honest. asyncpg was chosen over `psycopg`/SQLAlchemy-core specifically for native `pgpass.conf` resolution (see below) and because it is the thinner layer of the two for a project that wants raw SQL, not a query builder. **Consequence for every future router:** call the view or function directly (`SELECT * FROM v_profile_readiness WHERE ...`, `SELECT save_profile_weights($1, $2, ...)`); never re-model a table or reimplement a rule the database already enforces | Built `backend/app/` (`config.py`, `db.py`, `main.py`, `routers/health.py`), `backend/venv/` (Python 3.14.4 — asyncpg 0.31.0 has a working wheel, no compilation needed), `requirements.txt` frozen. `Settings` (Pydantic v2, `pydantic-settings`) reads `PGHOST`/`PGPORT`/`PGDATABASE`/`PGUSER` from the environment or `backend/.env` (gitignored, matching `.env.example`'s existing convention) — **deliberately no password field.** `asyncpg.create_pool()` is called with no `password=` argument at all, so it falls through to its own libpq-compatible resolution: `PGPASSWORD` env var, then the pgpass file, including the Windows `%APPDATA%\postgresql\pgpass.conf` location — same file `psql` already reads, same rule as every prior session ("don't ask for the password; don't write it into a file that isn't this one"). Confirmed working: the pool connected and `/api/health` returned `200` with no password anywhere in the process's own configuration. **Routes are mounted under `/api`** (`/api/health`, not bare `/health`) — required, not stylistic: `frontend-angular/proxy.conf.json` forwards `/api/*` without rewriting the path, and `environment.ts`'s `apiBaseUrl` is `/api`, so a bare `/health` would 404 through the real request path every screen actually uses. CORS restricted to `http://localhost:4200` (Angular's real default port — not the 4300 used ad hoc during Stage 5 testing) via `CORSMiddleware`; verified a non-listed origin gets no `Access-Control-Allow-Origin` header at all, not a wildcard. **The "done when" bar needed a small, in-scope frontend addition to be checkable at all**: `ApiClientService.getHealth()` has been typed since Stage 5 but nothing ever called it, so no screen's error state could possibly clear from building `/health` alone. Added `shared/backend-status.component.ts` — a header badge, not a new screen or business feature — calling the already-typed method. Verified end to end: `ng build` clean, `ng serve` on port 4200, browser network log shows `GET http://localhost:4200/api/health → 200 OK` (through the real dev-server proxy to `:8000`, not a mock), badge reads "Backend connected", zero console errors other than the still-expected `/vulnerability` 404 (Stage 2.1–3 not built) | **Resolved — Stage 2.0 done, 2.1–2.4 next** |
| 2026-08-11 | **T2 review — the 503 path was not merely untested, it was unreachable** | The T2 session reported the database-failure branch as "implemented, verified by code review only, didn't force a real outage". The code review reached the wrong conclusion. `lifespan` awaited `create_pool()` with `min_size=1`, which connects eagerly, and let the failure propagate — so with PostgreSQL **down at startup** the app did not boot at all, and `/api/health` answered with connection-refused rather than 503. The handler could only ever fire if the database died *after* a successful start. The common case — service not running before the API — took the path that did not exist, which is the exact scenario the endpoint exists to distinguish. **The gap was not the exception handler; it was the state it could not be entered from.** Worth keeping as a lesson: "verified by code review" is a claim about code that was read, and the thing that was wrong here was the code that was *not* read — the startup sequence two files away | Fixed and **proven by running it, not by reading it**: pool acquisition moved out of the lifespan's failure path into `open_pool_or_none()`, which logs and returns `None`; `get_pool()` retries on demand, so starting PostgreSQL *after* the API now recovers without a restart. Two further defects found while fixing it. **(a) The exception string was returned to the caller** — `detail=f"error: {exc}"` on an unauthenticated, public endpoint, and asyncpg connection errors carry host, port, user and database name while an auth failure quotes the user back verbatim. Now logged server-side, with a bare `database: "error"` returned (NFR-4, default deny). **(b) The naive retry hammered a dead database** — every request paid a full TCP connect timeout, tolerable for `/health` alone but not from T3 onward, when every router shares this dependency and a dead database would hang the whole API instead of failing fast. Added a 5-second cooldown. Verified in a sandbox with no database at all, which is the one environment where this is easy to test: app boots with the database unreachable; `/api/health` returns **503** with **no infrastructure token in the body** (host, port, user, database name and the OS error string all checked for); 20 further calls complete in 0.054s rather than 20 connect timeouts; and a pool appearing later flips the endpoint to **200 with no restart**. CORS re-checked at the same time — a disallowed origin gets **no** `Access-Control-Allow-Origin` header rather than a wildcard. Third fix, unrelated: **`.env.example` advertised a `PGPASSWORD=` key that silently does nothing** — pydantic-settings parses the `.env` file itself and does not export to `os.environ`, and `config.py` has no password field, so a value there is read by nobody and asyncpg never sees it. Anyone filling it in would get an authentication failure with nothing to explain it. Key removed and replaced with an explanation of why it is absent. Also confirmed against the installed asyncpg source, rather than assumed, that `compat.get_pg_home_directory()` returns `%APPDATA%\postgresql` on Windows — the pgpass route the whole no-password design rests on genuinely works | Resolved — T2 complete |
| 2026-08-11 | **T2b — auth built end to end: landing page, sign-in, registration, server-side sessions** | T3 onward needs a signed-in user and a province to authorise every write against (§3.2). Two decisions were given, not re-opened: sessions server-side in a new table with only an opaque id in the cookie, and passwords hashed with argon2id. Held to the same bar as T1/T1b/T1c: a negative test per constraint, applied live, registered in `apply_native.ps1`, proven from a cold build | **`schema_session_addendum.sql`** (new addendum, working rule 4): `app_session(id, token_hash CHAR(64) UNIQUE, user_id FK, created_at, expires_at, revoked_at, revoked_reason)`; `token_hash` is a SHA-256 digest of the cookie value, the raw token is never stored (same reasoning as hashing passwords, just fast rather than slow since the token is already high-entropy); `app_session_revocation_explained` CHECK ties `revoked_at`/`revoked_reason` together; `app_session_expires_after_creation` CHECK; `v_active_session` (unrevoked AND unexpired AND owning account still `active`); trigger `app_user_revoke_sessions_on_deactivation` fires on any `app_user.status` transition away from `active` and revokes every open session for that user — this is what makes "an account can be deactivated or rejected after approval" true at the database layer, not just at whichever API call happens to call it first. `backend/db/session_test.sql` — 9 checks including the two-fixture positive case (pending→active must NOT revoke a session that predates approval, so the trigger isn't over-firing). Registered as step 7/9 in `apply_native.ps1` (after auth, before boundary-pending — renumbered 1/8…8/8 → 1/9…9/9); proven via a scratch database through all 9 steps plus all four negative-test suites, then dropped; re-applied and re-tested on live `riskradar`. **Found and fixed a real, previously-latent bug in `check_ddl.py` while wiring this addendum's `DEPS` against `app_user.status`** (a column added via a 7-column `ALTER TABLE ... ADD COLUMN` in `schema_auth_addendum.sql`, not `schema.sql`'s own `CREATE TABLE`): `AlterTableStmt` column tracking compared `str(c.subtype)` against `"AT_AddColumn"`, but this Python/pglast combination's `AlterTableType` enum is an `IntEnum`-style member whose `__str__` (Python 3.11+ changed `IntEnum.__str__` to `int.__str__`) returns `"0"`, not the name — so every multi-column `ALTER TABLE ADD COLUMN` in the whole DDL chain had been silently entering nothing into the symbol table since whichever addendum first used one, undetected until a `DEPS` entry actually depended on such a column. Fixed to use `c.subtype.name`. `generate_erd.py`: `app_session` added to the "Auth & users" module; `revoked_at`/`expires_at` added to `ALWAYS_SHOW` (`token_hash` deliberately excluded, a digest is not something a reader wants on the diagram). **`app/security.py`** — `argon2-cffi`'s `PasswordHasher` for `hash_password`/`verify_password`; `secrets.token_urlsafe(32)` + SHA-256 for session tokens. **`app/deps.py`** — `db()` (503 if the pool is `None`, same graceful-degradation contract as `/api/health`), `get_optional_user`/`get_current_user`/`require_admin` built on `v_active_session`. **`routers/auth.py`** (`POST /auth/register`, `/auth/login`, `/auth/logout`, `GET /auth/me`) and **`routers/admin.py`** (`GET /admin/registrations`, `POST /admin/registrations/{id}/approve`, `/{id}/reject`) under `/api`. Registration `type` (`agency`/`expert`/`public`) maps to role code (`data_officer`/`expert`/`community`); `agency`/`expert` require `province_id`, `public` must not set one, both enforced server-side regardless of what the client sends. **Reject is also the revocation path** — deliberately not restricted to `status='pending'`: BUILD_BRIEF's endpoint list has no separate "deactivate", so rejecting an already-active account is how an administrator forces a sign-out, and it works because the session trigger above doesn't care which caller changed `status`. Login gives the same "invalid email or password" for an unknown email and a wrong password (no account-existence enumeration). **Bootstrapped the first admin** via `backend/db/bootstrap_admin.ps1` (the chicken-and-egg problem: nobody can approve the first admin through an API that requires an admin to call it) — password hashed via the venv's own `app.security`, passed through an environment variable rather than a command-line argument or SQL string so it can't break out of either; SQL built from a single-quoted PowerShell here-string with `{{TOKEN}}` placeholders after the first attempt used a double-quoted one and PowerShell tried to expand PL/pgSQL's literal `$$` dollar-quote delimiter as its own `$$` automatic variable. **Angular**: `AuthService` (session-state signals, `refreshMe()`/`login()`/`logout()`/`clearAuth()`, role-based landing route), `adminGuard` (re-checks the server before rendering, not just the last-known signal, and redirects to `/login` on a guard-observable error rather than letting it fail navigation outright), landing/login/register/admin-registrations screens, `api-error.interceptor.ts` now clears auth state on any 401 (FR-5.21). `app.routes.ts`: `/` is now the landing page, the map moved to `/map` (still reachable with no account, FR-12.7). **Province lock (FR-12.8/§3.2)**: `profile-context.component.ts` presets and disables the province `<select>` for `data_officer`/`expert`; matched against the account's DB province name through a spacing/case-insensitive comparison rather than an exact string match, because `reference-data.model.ts`'s `PROVINCES` list already disagreed with the database on one name (`"Northwestern"` vs. seeded `"North Western"`) before this session touched it — flagged as a separate out-of-scope task rather than fixed here, since fixing it touches the shipped GeoJSON asset's own `province` property and the map's existing filter matching. Also used an `effect()`, not just the signal's initial value, to reapply the lock once `AuthService.refreshMe()` resolves — the initial value alone would miss a direct page load landing on `/entry` before that async call settles. **End-to-end proof, twice**: `curl` against the running API (register → 403 on a pending login → admin login → list → approve with role+province → double-approve 409 → non-admin 403 on `/admin/*` → logout leaves the session truly revoked, a subsequent `/auth/me` is 401, not merely a cleared cookie → admin **reject**ing an already-**active** account revokes its live session via the database trigger alone, proven by hitting `/auth/me` with that still-open cookie and getting 401 with no `logout` ever called) and then the same chain again through the actual browser (register → admin approves in the UI → sign in → role-redirect to `/map` for a community user and to `/entry`, locked to "Western", for a data officer → admin-driven reject on the live tab flips the header back to signed-out and unlocks the province field on the next navigation, with no manual sign-out). `ng build` clean. All test-fixture accounts deleted from the live database afterward; only the bootstrap admin remains | **Resolved — Stage 9.6 done, T3 next** |
| 2026-08-14 | **The official division count moved again — so it was removed from the documents rather than updated** | Milinda: the official DS-division total is now **340**, against a register of 331. That is the *third* count in three weeks (330 shapefile → 331 Kalmunai split → 340), and the second time a sweep of prose, seeds, assets and tests would have been needed to chase it. The 10 Aug sweep to 331 was done correctly and was stale in four days. The problem is not the number, it is that the number was written down at all | **Made the count agnostic instead of updating it.** §5.1 now names three distinct quantities — *official* (what the Government recognises), *registered* (`dsd_register.csv`, the denominator for every completeness rule in §2.2) and *drawable* (`geom IS NOT NULL`) — and states that all three are read at runtime and none is a live figure in the document. Swept: `SRS_v2.3.md` (§1.1, glossary, §5.1, §6.6 verification, §9 status, Annex B, O-6), `CLAUDE.md`, `PROJECT_GUIDE.md`, `FRONTEND_BUILD_GUIDE.md`, `UI_REDESIGN_BRIEF.md`, `backend/README.md`, `apply_native.ps1`, `check_prereqs.ps1`, `load_spatial.ps1`. **`smoke_test.sql` rewritten off literals onto invariants**: surveyed + pending = registered, surveyed ⇒ geometry, pending ⇒ no geometry, every division resolves to a province and district — counts are now RAISE NOTICE, not RAISE EXCEPTION, because a test asserting 331 fails the day 340 lands and tells you nothing useful. Historical statements (what [P-2] specified, the 10 Aug decision rows) left exactly as written | **New open item [O-12]** — nine divisions unregistered, blocking the national index and any province containing one |
| 2026-08-14 | **A stale count had already reached the user interface** | The sweep found `filter-bar.component.ts` telling users *"the register holds 330 of the 331 official DS divisions (missing one in Ampara district, Eastern province)"* — wrong on both numbers and on the cause, five days after O-2 closed. This is the failure mode that matters: a hardcoded count in a comment is a nuisance, the same count in a user-facing explanation reads as authoritative | `nationalScopeDisabledReason` is now a `computed()` over a new `DivisionCoverage` (`official` \| `registered` \| `drawable` \| `withValues`) served by `ReferenceDataService.getDivisionCoverage()`, which returns **`null` until the API answers and has no fallback value on purpose** — callers handle `null` by saying less, never by guessing. Stale comments corrected in `boundary.service.ts`, `map-page.component.ts`, `reference-data.model.ts`. `GET /reference/division-coverage` is now owed by Stage 2 | Resolved — endpoint outstanding |
| 2026-08-14 | **"What remains to upload" became a requirement, not a report** | Milinda's position on the weights and phase-2 backlogs: they close gradually as real workbooks arrive, so they need no decision — *provided the user can always see what is outstanding*. The existing FR-2.12–2.16 province import summary covers this only for a province that has just been imported, which is the wrong shape: it answers after an upload, and the question is asked before one | Added **FR-2.17** (a standing outstanding-data register, available without running an import, resolved to profile × period × variable × DS division, distinguishing *not yet supplied* from *supplied and rejected* from *not applicable*) and **FR-2.18** (it must name what would discharge each unmet precondition — the divisions still holding no value, the hazard components held under [P-5], the 30 exposure groups short of 100, and any division in the official count but not in the register — exportable, province-scoped for a data officer and national for an administrator) | Resolved |
| 2026-08-14 | **Annex D regenerated — it was stale by 13 requirements, not the 3 recorded** | The 9 Aug row noted Annex D as stale after three FRs were added. Counting definition rows in §6 found it understated four groups: FR-1 (10, not 8), FR-4 (23, not 19), FR-5 (27, not 26) and FR-12 (10, not 6). Nobody had added FR-12.7–12.10 to the annex when auth landed on 11 Aug | Regenerated by script against the §6 tables: **168 functional + 20 non-functional = 188**. Added a note that a count legitimately exceeds its ID range wherever suffixed IDs were inserted (`FR-1.4b`, `FR-4.8c`…), because that is the §6 identifier rule working, not an error — the previous table gave no way to tell the two apart | Resolved |
| 2026-08-14 | **The measurement model closed — O-10 and O-11 answered together, and the answer was neither of the ones on the table** | Both open items were being held for "real values at stage 4": whether the second rescale still earned its place (O-11), and where the band thresholds should sit (O-10). The advice on record was to derive thresholds from the observed distribution once a province loaded, because the raw product is right-skewed and even fifths would leave the bottom band holding ~46% of divisions. Milinda decided the opposite: **renormalise to 0–1 and assign the colours to that scale**, final | **Both closed; [P-4] and [P-14] confirmed.** The index is `H × E` min–max rescaled to [0, 1] within its province and the bands are the even fifths of it — 0.2 / 0.4 / 0.6 / 0.8 — **fixed, and explicitly not to be re-derived from data**. Quantile and natural-breaks classification rejected. **The decision is right for a reason the earlier advice under-weighted:** data arrives in instalments over months, so any data-derived classification re-cuts its bands each time a province completes — the same colour would mean different numbers at different times, and a map exported today would disagree with the same map next quarter without either being wrong. A fixed legend is a precondition for publishing at all, and that outranks the cosmetic gain of a filled colour spectrum. The bottom-heavy distribution is accepted as an honest reading: forcing 20% into the top band by quantile manufactures severity the measurement does not support. Edited: §2.1 (both notes), §2.7 rewritten with the reasoning and the accepted cost, §10 stage 4 gate removed, **FR-4.13** rewritten off "placeholder", **FR-4.15** re-scoped to monitoring, **new FR-4.13b** for the colour ramp, O-10/O-11 struck through in §11.3, Annex C P-4 and P-14 marked confirmed, Annex D 169 + 20 = 189 | Resolved — **no open questions remain in the measurement model** |
| 2026-08-14 | **Band colours specified, and validated rather than chosen by eye** | §2.7 had five band *names* and no colour codes at all, so "assign the colour codes accordingly" had nothing to attach to. A vulnerability choropleth also has a constraint most charts do not: the three coverage states must be distinguishable from every band colour (FR-5.14), and the lowest band must not recede into the surface, or *very low* reads as *no data* — the exact defect the retired React client shipped | A **single-hue sequential ramp**, severity carried by **lightness rather than hue**, so it survives colour-vision deficiency and greyscale print. Light and dark variants, the dark one re-stepped rather than inverted: in both modes *Very high* is the most prominent step and *Very low* the least — on light that means darkest, on dark it means lightest, so salience direction is preserved and lightness direction is not. Both sets validated programmatically, not by eye: monotone lightness, adjacent ΔL ≥ 0.06, hue spread ≤ 1°, and **the step nearest the surface clearing 2:1 contrast** — that last check is the one that keeps *Very low* from being read as absent. Landed as `frontend-angular/src/app/core/models/band.model.ts` with `bandFor()`, whose boundary cases are unit-checked; it returns **`null` for an absent score, never *Very low***. `COVERAGE_STATE_FILLS` is exported alongside so a reviewer can see the two sets are disjoint rather than take it on trust | Resolved |
| 2026-08-14 | **The 340-division shapefile arrived — and the nine "new" divisions turned out not to be new** | `DS_Boundary.rar` (Survey Department, 2025-10-09) delivered 340 polygons against a register of 331. The obvious reading was nine divisions previously missed. Overlaying the new polygons on the old 330-polygon set — rather than matching names — showed something else: **every one of the nine is carved out of a division that already existed**, and two parents (Ampara aside) vanish entirely | Register regenerated at 340. Parentage recorded per row in a new `split_from` column and in SRS §5.1: Ambagamuwa → Ambagamuwa Korale + Norwood; Kothmale → Kothmale East + West; Hikkaduwa → Madampagama + Rathgama (+ remainder); Baddegama → Wanduramba; Hanguranketa → Mathurata; Walapane → Nildandahinna; Nuwara Eliya → Thalawakele; Balangoda → Kalthota. **Two consequences that name-matching would have hidden.** (1) The eleven new units start with **no values** — copying a parent's double-counts every count and extent variable, the Kalmunai rule. (2) The six *surviving* parents now cover **less area than before**, so an extent or count value collected against the old boundary is wrong for them, not merely stale. Milinda confirms the 2020–2025 collection is tabulated against the 340 boundaries, so no remapping is needed | **[O-12] closed** |
| 2026-08-14 | **Official DS codes adopted — [O-5] closed after three weeks open** | The shapefile carries `New_DS_Cod` (KA1, AM8, GA14) and a numeric `DS_Code`. `PROJECT_GUIDE.md` had said since 26 July: *swap for official codes before bulk data entry if they exist.* They exist, and no bulk data has been entered — so this was the last moment it could be done as a rename rather than a re-key of every uploaded value | `schema_official_dscode_addendum.sql` (Stage 1.15). **Renames in place rather than delete-and-reload**, because `ds_division.id` is referenced by `indicator_value`, `vulnerability_result`, `community_rating` and the toolbox tables — a reload would cascade those away or fail on the FKs, and would hand new ids to divisions that had not changed. Old codes kept as `legacy_code` so a stale `CEN-001` reference can be diagnosed instead of silently returning nothing; **never a join key**. `EAS-008`, `CEN-032` and `CEN-034` retired and never reused | Resolved |
| 2026-08-14 | **The migration's own guard caught a case I had missed — twice** | Written, then tested against a scratch PostgreSQL 16 seeded to the pre-migration 331-row state rather than reasoned about. Two real defects surfaced. **First**, the generator put the row-separating comma *inside* the trailing `--` comment, so the entire `VALUES` list was malformed — it would have failed on first run against the live database. **Second**, after fixing that, the migration's own assertion refused to commit: *2 division(s) still carry a generated code*. Ambagamuwa and Kothmale have no successor row at all — unlike the six parents that survived, those two names are gone from the official register | Added step 1b: retire them explicitly, **guarded** — if either holds any indicator value the migration raises instead of deleting, because splitting a division that already has data is a remapping decision, not a migration. Verified end to end: two consecutive runs both succeed (idempotent), final state is 340 divisions, 0 generated codes left, 0 duplicates, 0 null provinces, `CEN-001` resolves to `KA1`, `CEN-032` resolves to nothing as intended; and a negative test confirms the data guard fires. **The lesson is not the two bugs — it is that neither was visible by reading the file** | Resolved |
| 2026-08-14 | **The loader joined on name, which the 2025 revision would have broken silently** | `load_spatial.ps1` matched `stg_dsd.adm3_en = stg_register.ds_division`. Nine divisions were renamed in this revision — "Koralai Pattu North" became "KORALAI PATTU NORTH (VAHARAI)" — so those rows would have loaded as **boundary_pending with a perfectly good polygon sitting unused in staging**: no error, no map, and a coverage statement quietly reporting nine divisions as unsurveyed | Join moved to `new_ds_cod = ds_code`, the official code, which is stable across renames. Also: source switched to `DS_Boundary.shp`, `stg_register` DDL widened for the new columns, and **`-s_srs EPSG:5234` stated explicitly** — the Kandawala .prj carries no EPSG authority code, and a misread source CRS places every polygon in the wrong part of the world without raising anything. Reprojection verified against the old boundaries: median centroid offset 244 m, median shape overlap 0.92. The "expected non-zero" comment block was rewritten — both orphan counts should now be **zero**, and must not be relaxed back | Resolved |
| 2026-08-14 | **Shapefile accepted against the DSD_REGISTER_SPEC checklist, not on trust** | The spec written on 26 July lists six acceptance checks. All six run and pass: 340 records; 25 districts and 9 provinces all resolving to existing names; **0 duplicate names and 0 duplicate codes** on both code fields; **0 invalid and 0 zero-area geometries**; polygons tile the country with 2.9 km² of sliver overlap on 66,037 km² (**0.004%**); `.prj` present and recognised. Computed national area 66,034 km², within 0.7% of the commonly quoted 65,600 km² | Accepted. One data-quality note carried forward: an embedded newline inside `KORALAI PATTU SOUTH (KIRAN)` in the DBF, stripped on ingest |
| 2026-08-15 | **The build's overlap check failed on the new shapefile — and it was right to** | `smoke_test.sql` reported *2 overlapping DS-division pairs*. The tempting fix was to raise the 1e-7 sq-degree threshold until it passed. Measuring first showed why that would have been wrong: of 49 touching pairs, 47 are under 20 m² of digitising noise, but two are real — **Palugaswewa ↔ Higurakgoda at 2.110 km²** and **Elahera ↔ Naula at 0.759 km²**. Both are inter-district boundaries traced differently by the agencies either side: 474 and 228 fragments strung along 24.2 km and 13.7 km, averaging under 100 m wide — ribbons, not disputed land | **Pinned by code, not absorbed by threshold.** A threshold loose enough to admit 2.11 km² would also admit a genuine new 2 km² overlap anywhere in the country, silently. The two pairs are named in the exclusion, so a *third* overlap still fails; and a second assertion fails if either known pair grows beyond 2.2 km², so a future boundary revision that worsens it is caught rather than absorbed. Verified against a PostGIS fixture in all three states: an unknown pair fails, the two pinned pairs pass, and a pinned pair grown to 48.9 km² fails. Recorded in SRS §5.1 as the Survey Department's to correct — the effect is that toolbox area apportionment double-counts 2.9 km² of 66,034 (0.004%); vulnerability scores are keyed by division, not area, and are unaffected | Resolved — defect reported, not patched |
| 2026-08-15 | **Three build failures in a row, all mine, all invisible to reading** | The 340-division migration failed at `-Reset` three times: a **UTF-8 BOM** written by `Set-Content -Encoding UTF8` on Windows PowerShell 5.1 (psql read it as part of `DROP`); **client_encoding WIN1252** rejecting the register's Sinhala names at line 5; and a smoke-test assertion referencing **`district_id`**, which does not exist — the column is `district_name`. Each was a one-line fix and none was visible without running it | Fixed and, more usefully, tested the way they should have been the first time: a scratch PostgreSQL 16 + PostGIS in the sandbox, seeded to the exact pre-migration state, exercising **both** the cold-build and in-place-migration paths plus negative cases. That harness caught two further defects before they reached Milinda — a row-separating comma swallowed by a trailing `--` comment, and `%.3f` in a PL/pgSQL `RAISE`, which only supports a bare `%`. **The lesson: encoding and environment bugs do not survive being run, and do not surface any other way.** Both `apply_native.ps1` and `load_spatial.ps1` now set `PGCLIENTENCODING=UTF8` explicitly; the BOM-prone `Set-Content` is replaced with `UTF8Encoding($false)`. Note the WIN1252 trap was **always latent** — the pre-14-August register had an identical undefined-byte profile, so an earlier successful load merely ran in a console that happened to be on a UTF-8 code page | Resolved |
| 2026-08-15 | **`save_profile_weights()` silently discarded exclusions — and the first write-up of it was wrong** | T3 needed the function to carry `consensus`. It did not: the INSERT listed only weight and relationship, so every save reset the column to its default `agreed`. I first reported the consequence as cosmetic — *"nothing corrupt could be stored, a CHECK caught it, the user just got a poor error message"*. **That was wrong, and running it proved it.** The CHECK `profile_indicator_inactive_unweighted` only constrains rows that ARE rejected; since the old function never marked anything rejected, it never fired | Demonstrated by recreating the original as `save_profile_weights_old` and running the identical payload through both. Given an exposure domain reaching 100 **only** by counting a variable the expert had excluded, the old function **succeeded**: it stored `ASWEDDUMIZED_PADDY_EXTENT` as `agreed` at 45%, discarded the exclusion note, reported `n_rejected = 0`, `exposure_total = 100.000`, `is_computable = true`, and left a new active version V9 behind. A published score would have included a variable an expert explicitly ruled out, with nothing in the data to show it had ever been ruled out. **This is a correctness defect, not a UX one.** The same payload on the new function raises before writing anything. Second demonstration, same two functions: identical valid input flips `is_computable` from **f** to **t**, because the old one leaves the composite index counted as an unweighted `agreed` member and the new one records it as `rejected` and attributed | Resolved — `schema_profile_consensus_save_addendum.sql` |
| 2026-08-29 | **Sheet protection blocked the expert review it was meant to survive** | The 243 collection workbooks ship with `ws.protection.sheet = True` on both period tabs and WEIGHTS, so only the yellow value cells and the PROPOSED WEIGHT column accept input. That is right for collection, but the panel is still revising the templates themselves — they need to rename a column, drop a variable, mark up a header — and protection made that impossible. No password was ever set, so the lock was pure friction, not security | Protection made conditional rather than removed: `generate_templates.py` gains **`--unlocked`**, which sets `ws.protection.sheet = not UNLOCKED` on both fill functions, writes `protection = unlocked-review` into the hidden `_META` tab, and rewrites README items 3 and 5 so the copy invites structural mark-up instead of forbidding it. All 243 regenerated in place with the flag; `verify_workbooks.py` PASSes against the 340-row register and `rebuild_manifest.py` reproduces the same 243 rows, so nothing but the lock changed. The locked set is one re-run away — `python generate_templates.py` with no flag — and `Templates_generated_LOCKED_backup.zip` holds the previous build. **Re-issue locked once the panel returns the finalised version**; an unlocked workbook must not be the one collection runs on, because the importer validates against `_META.expected_columns` and a well-meant column rename would fail every row | Resolved — unlocked pending panel sign-off |
| 2026-09-03 | **Central's returned review carried a catalogue change, not just mark-up — and real data** | The panel returned all 33 Central workbooks. Three things came back at once. (1) They renamed both period tabs to **2021-2025 / 2026-2030** but could not change the YEAR_START/YEAR_END cells, which were locked — NOTED.xlsx says so explicitly. (2) They inserted **10 new hazard/exposure columns** on the data tabs, splitting four combined-period variables into 25%/75% sub-periods and adding `3DAY_CUMULATIVE_RAINFALL` and `VERY_WET_DAYS_95TH_PERCENTILE`; the four superseded parents were left **completely empty in every profile** while the splits were filled, which is what identifies them as retired rather than merely unfilled. (3) The tabs hold **actual Central data**, ~12,700 values — this is the first real dataset, not a marked-up template | Folded back in as configuration, not by editing the experts' file. `build_province_overrides.py` derives two sidecars from the returned workbooks so the derivation is reproducible and re-runnable when the next province reports: **`catalog_addendum.csv`** (10 definitions, merged by `load_catalog`, leaving FINAL_VARIABLES.xlsx untouched) and **`province_variable_overrides.csv`** (116 add / 58 retire / 4 weight rows, applied per province on top of national membership). `PERIODS` is now `(2021,2025),(2026,2030)` for all nine provinces — contiguous, so README 2b's *later-period-wins* tie-break is gone and no year can be double-counted. Where a retired parent carried a weight, the 25/75 split is **derived** so each domain still totals 100, and a new WEIGHTS column G states the basis of every proposal (`derived: 25% of retired X (40)` vs `panel: entered in the returned WEIGHTS tab`) so a derived number is never mistaken for an expert decision. All 243 regenerated; `verify_workbooks.py` PASSes, all 33 Central profiles match the panel's column set exactly (only the retired parents absent, nothing extra), **0 short domains in Central** (the 26 that remain are the known pre-existing exposure backlog in the other eight provinces, down from 30 because the panel closed Central's four), and the other 210 workbooks changed only in period range. Two calls from Milinda on the day: the panel's hand-typed `3DAY_CUMULATIVE_RAINFALL` is renamed **`THREE_DAY_CUMULATIVE_RAINFALL`** — a leading digit is not a legal SQL identifier and would need quoting in the catalogue, the importer and every generated column forever; the original header is recorded in the addendum note as the `indicator_alias` to keep, so the panel's returned files still match. And the **tab name is authoritative for the period**: the returned files' YEAR_START/YEAR_END cells still read 2020/2025 because they were locked, and are to be disregarded — whatever imports that data reads the tab | Resolved — derived weights await panel confirmation |
| 2026-09-03 | **The override derivation quietly produced nothing, and reported success doing it** | `build_province_overrides.py` works out what a panel changed by diffing their returned workbooks against the build they were sent. Its baseline pointed at `design/templates/generated/Central` — which the generator had just rewritten *with the overrides already applied*. Re-running it after the rename therefore diffed the panel's files against themselves: **10 definitions became 5, and 116 add / 58 retire / 4 weight rows all became 0.** The script printed those zeros as a normal result and exited successfully. Had the regeneration run on that output, all 33 Central profiles would have reverted to the pre-review catalogue with nothing to show it had happened | Baseline pinned to a folder that cannot move: `design/templates/baseline_2026-08-15/Central`, extracted from `Templates_generated_LOCKED_backup.zip` — the 15 August locked build the panel was actually sent — with a README saying not to regenerate it and why. **The wider lesson: a diff-derived config file needs an immovable baseline, and an empty diff must never be indistinguishable from a clean run.** Caught only because the row counts were checked against the previous run rather than the script's exit status | Resolved |
| 2026-09-03 | **A 1:1 rename that was really a composite split — and would have dropped 60% of a hazard variable invisibly** | Folding the Central panel's changes in, `VERY_WET_DAYS` was read as simply renamed to `VERY_WET_DAYS_95TH_PERCENTILE`, taking the whole of its weight, with `3DAY_CUMULATIVE_RAINFALL` filed as a new unparented variable and left unweighted — and an unweighted variable is **excluded from computation**. The legacy variable's own name says otherwise: *"Very wet days (95th percentile) **and** 3 day cumulative rain fall (out of 35 — 40% for very wet days and 60% for 3 day cumulative respectively)"*. It was a composite, the panel separated it into its two components, and both appear in exactly the same 18 profiles it did. The error would have discarded the 60% component of a flood and landslide hazard variable **while the domain still totalled 100**, so no check would have fired and nothing would have looked wrong | Split 40/60 per the name the catalogue already carried: in Paddy/Flood the parent's 35 becomes 14 + 21. Found by reading the catalogue row rather than the code — the variable *name* was carrying the specification. **Worth generalising: a legacy label that reads like a sentence is often a spec, not a label**, and the 25/75 event splits arrived the same way, in the panel's column headings. Confirmation requested from the panel | Resolved — 40/60 applied, panel to confirm |
| 2026-09-03 | **The >= 0 rule was right in general and wrong for exactly two variables** | Every template declares raw values must be >= 0 and the reader enforced it globally. On its first real payload it rejected **498 of 14,020 values in all 33 workbooks** — but only across two variables: `THREE_DAY_CUMULATIVE_RAINFALL` (−30.05…57.05, **median −0.01**) and `OCCURRENCE_WARM_DAYS` (−1.25…3.50). A median of −0.01 is not a rainfall depth; both are changes or trends, where a negative number is meaningful. Every other variable in the same files is strictly positive — very wet days 102.8–139.5, SPI 1.44–1.88 | Made per-variable, **not** relaxed globally: `schema_signed_values_addendum.sql` adds `indicator_catalog.value_kind` (`absolute` / `signed`) and the reader only permits a negative for a declared signed variable. A blanket relaxation would have silently accepted a negative population count or land extent — the class of defect the rule exists for. The enum says only `signed` rather than guessing between anomaly and trend, which is with the panel. **Normalisation is unaffected** (min-max is offset-invariant); only DIRECTION could be wrong, and that is one column plus a recompute — cheap because normalisation is server-side | Resolved — loaded, sign convention with the panel |
| 2026-09-03 | **An alias silently folded a new variable back into the one it replaced** | The template loader applied every `indicator_alias` row to incoming column headings. But `VERY_WET_DAYS_95TH_PERCENTILE` was a legacy *alias* for `VERY_WET_DAYS` before the panel made it a variable in its own right, and `PCT_LANDSLIDE_AFFECTED_AREA` and `LANDSLIDE_AFFECTED_AREA` both alias the same code. So the panel's new columns resolved back onto the retired parent, and two columns collapsed onto one — reported as "columns missing" and "duplicate", which reads like a bad file rather than a bad rule | An alias may never override a real code: the lookup now excludes any alias whose text is itself a catalogue code, and two columns resolving to one variable is an explicit error rather than a silent last-write-wins. Also fixed alongside: review copies were being held to the locked column ORDER, which the panel had been explicitly invited to change — order is now enforced only for locked issues, and a column absent from a review copy is a warning, not a rejection | Resolved |
| 2026-09-03 | **`app/importer` cannot read our own templates, and that is correct** | Seeding Central revealed that the Stage 3 importer library parses only the **legacy** nine-province workbooks: multi-block sheets, zone label rows, numeric `DS_Code`, free-text labels needing reconciliation. It cannot read the generated upload templates at all | Not a defect — it is the dual-path ingestion settled in D3 rev 5. Added `template_reader.py` (strict path: `_META` is the contract, so a renamed, reordered or inserted column is a named structural error) and `load_template.py`, which the seed and the forthcoming upload endpoint both call so they cannot diverge. **Result: all 33 Central workbooks loaded — 14,020 cells, 4,412 distinct values, 41 divisions, zero rejected, and where two workbooks carried the same figure they agreed every time (0 conflicts).** The 14,020-to-4,412 ratio is expected: a raw value is keyed to variable × division × period, not to profile, so a variable shared across profiles is one fact, not many | Resolved |
| 2026-09-03 | **Stage 4 engine built and run on real data — 33 of 33 Central profiles compute** | Normalisation, direction inversion, marked carry-forward, H and E, `V_raw = H x E`, provincial rescale, and refusal. **1,323 result rows.** Verified: recomputation is **bit-identical** (0 differing rows across all 1,323); all 33 profiles put exactly one division at **1.000**, the signature that the provincial rescale ran; unassessed divisions carry **no row at all** rather than a zero; and four refusal cases are asserted by `tests/test_engine_refusal.py`, not by inspection — an unweighted variable refuses and **names** it, a domain at 95 refuses rather than rescaling to 100, a division stripped of data comes back unassessed, and a positive control proves the profile computed before anything was broken. Direction inversion is not academic: **131 memberships are `higher_is_better`** | `app/engine/vulnerability.py` plus `compute_all.py`. Coverage is good: 22 of 33 profiles score all 41 divisions, the rest 37-40, and the shortfalls are honest — NU5/NU6/NU7 have no livestock returns, MA1/MA6/MA7/MA9 no landslide-area figures | Resolved |
| 2026-09-03 | **"Exactly one 0.000 per profile" does not survive contact with real data, and should not** | The stated Stage 4 exit signature is one 1.000 and one 0.000 per province per profile. The 1.000 holds in all 33. The 0.000 holds in only 26: **90 divisions score exactly zero**, concentrated in seven profiles — 19 in each inland-fishery profile, 10 in each pig-and-sheep one. This is not a tie-breaking bug. Those 19 divisions have **no inland fishery at all**: zero fishermen, zero production, zero tanks, zero stocking. Exposure is genuinely 0, so `V_raw = H x 0 = 0`, and every such division lands on the floor together | The arithmetic is right and the exit criterion is what needs restating: **exactly one 1.000 always; exactly one 0.000 only where the provincial minimum is unique.** A structural zero is a real reading, not a defect. **But it raises a live question for the map:** `0.000` is documented to mean *least vulnerable in this province*, and for these divisions it actually means *this sector is not present here*. Rendering them in the lowest band puts a division with no fishery on the same footing as one with a thriving fishery that is coping well — the absent-vs-zero distinction, one level up. Referred to Milinda; the engine stores the honest zero either way, so this is a presentation decision, not a recompute | Open — presentation decision |
| 2026-09-03 | **The map's filter constants had drifted from the database on three axes at once** | `core/models/reference-data.model.ts` carried the taxonomy as literals that must agree with the database but were maintained where nothing could check them. All three were wrong by the time the API existed: **subsectors were DISPLAY NAMES** ('Cattle', 'Pig & Sheep', 'Poultry farming') where the API keys on codes (`CATTLE_FARMING`, `PIG_AND_SHEEP_FARMING`, `POULTRY_FARMING`), so every livestock selection would have 404ed; **provinces were names with no code**, sending 'Central' where `CEN` was expected; and **PERIODS still read 2020-2025 / 2025-2030**, superseded that morning. Each failure surfaces to a user as *missing data* rather than *stale client* | Added `GET /api/reference/taxonomy`, serving provinces, sectors with their subsectors, hazards, the periods that **actually hold results**, and the register counts — sector/subsector pairs limited to those a profile exists for, so the control cannot offer a combination that 404s. `TaxonomyService` loads it with **no hardcoded fallback**: a wrong list is worse than an absent one, because populated controls that all fail read as broken data. Also fixed while there: `[title]="nationalScopeDisabledReason"` was binding the signal itself rather than calling it | Resolved |
| 2026-09-03 | **Two definitions of the band ramp, and the map was using the unvalidated one** | `band.model.ts` carries a **single-hue** sequential ramp — severity by lightness, validated for monotone L, adjacent separation >= 0.06, hue spread <= 1 degree, and 2:1 contrast on the lightest step — with a comment saying in terms not to swap colours by eye. `features/map/coverage-style.ts` had its own **green-yellow-red multi-hue** scale. The legend and the map could disagree, and the set actually painting the map was the one nobody had checked. Multi-hue also loses the colour-vision-deficiency and greyscale-print properties FR-4.13b exists for | `coverage-style.ts` now imports `BANDS` and `COVERAGE_STATE_FILLS` from `band.model.ts` and defines no colours of its own | Resolved |
| 2026-09-03 | **Coverage was counted against every polygon on screen, not against the province** | `recomputeFallbackCoverage()` took its denominator from however many boundaries the GeoJSON asset rendered — **340, nationally** — while the units belong to one province. A fully covered 41-division Central would have reported *41 assessed of 340*, i.e. 12% coverage of a complete province, in the statement users are meant to trust about completeness | Counted from the results instead: the API returns one unit per division of the province, scored or not, which is exactly the denominator the statement needs. The map also now **hides divisions outside the selected province and fits the view to them** — previously the other eight provinces rendered hatched, which reads as *no data collected* rather than *not part of this view* | Resolved |
| 2026-09-03 | **Results API and map wired end to end; Central renders** | `GET /api/vulnerability` follows SRS section 9 — a **filtered list**, not a scope in the path, which would have collided with `/vulnerability/{unit}` — and matches `VulnerabilityUnit` field for field including camelCase, so the typed client needs no translation. Verified against the replica: 41 units for Paddy/Flood with bands 30/3/4/2/2 (bottom-heavy exactly as `band.model.ts` predicts and accepts); Cattle/Drought returns NU5, NU6, NU7 as `state: unassessed` with **`value: null` and `band: null`** — an absent score never becomes band 1; Inland Fishery flags all 19 structural zeros; `indexScope=national` returns **409 with the reason** rather than an empty list; an unknown scope returns 404 naming the four codes. Frontend type-checks clean | One inconsistency left standing deliberately: **`routers/profiles.py` emits snake_case while `core/models/profile.model.ts` declares camelCase.** The new endpoints follow the frontend models and section 9. Changing profiles.py is a separate edit with its own blast radius, not something to fold into this one | Resolved — profiles.py casing still open |
| 2026-09-03 | **A sector that is not present in a division was rendering as *least vulnerable*** | 64 of Central's 1,323 scores are a true 0.000 reached because a whole domain is 0 — the sector simply is not there. **19 of Central's 41 divisions have no inland fishery at all** (no fishermen, production, tanks or stocking), 10 have no pig or sheep farming. The arithmetic is right; the rendering was not. In the lowest band those 19 divisions were indistinguishable from a division with a thriving fishery that is coping well, and the detail panel would have opened on them showing nothing — which reads as *data missing* when the truth is *question does not arise*. Distinct from the other 26 zeros, which are ordinary provincial minima | **Owner decision: such a division is not selectable and carries no score.** Added a fourth coverage state `not_applicable`, returned with `value` and `band` **null** while `hazardIndex`/`exposureIndex` still carry the zero that is the evidence for it. Rendered **flat** where `unassessed` is hatched — the two must not read alike: one says *we do not know*, the other *it does not arise* — with its own legend entry and its own count in the coverage statement. Clicks and the pointer cursor are suppressed. **Deliberately NOT changed:** the stored result keeps its honest 0.000 and the provincial min-max bounds still include these divisions. Excluding them from the rescale would move every other division's score, and the measurement model was closed on 14 Aug — that is a separate owner decision, and its cost is that variation among the 22 fishery divisions is compressed | Resolved |
| 2026-09-03 | **The Central panel's open questions: defaults confirmed rather than held** | Four questions went to the panel (`Expert Review/PANEL_QUESTIONS_CENTRAL_2026-09-03.md`) and Milinda's call was to proceed on the best reading rather than wait. Recorded so the reading is not later mistaken for the panel's own answer | Standing: both signed variables keep **`higher_is_worse`** — a wetter 3-day rainfall trend is worse for flood and landslide, more warm days worse for drought; the very-wet-days composite keeps its **40/60** split, read off the legacy variable's own name; the period splits keep **25/75**, the proportion the panel's headings state. The derived weights remain `contested`, not `agreed`, so the map can say the weighting is unconfirmed. **The exposure if a direction answer comes back inverted is real but bounded:** `THREE_DAY_CUMULATIVE_RAINFALL` carries 21 of the hazard domain's 100 points across 18 profiles, and correcting it is one catalogue column plus a recompute — cheap only because normalisation is server-side | Resolved — provisional pending the panel |
| 2026-09-03 | **The same official figure, imported twice by two people, became two rows — and the engine picked between them at random** | Found by running the new import endpoint against data the seed had already loaded: values went 4,412 -> 4,740 instead of staying put. `indicator_value_uniq` includes **`user_id`**, which is right for the expert and community tracks where each contributor's figure is its own record, and wrong for the official `data` track, where a value belongs to the division and not to whoever uploaded it. **328 duplicate facts, identical values, two users.** The engine reads `DISTINCT ON (ds_division_id) ... ORDER BY year_start DESC`, which had no tiebreak between them — so which row it used was arbitrary, and that **silently breaks the guarantee that recomputation is bit-identical** without raising anything | Fixed at both ends. The loader now deletes any existing `source='data'` row for the same variable / division / period before inserting, **in the same transaction**, so on the official track the latest import supersedes whoever ran it; and the engine's `DISTINCT ON` gained `id DESC` as a deterministic tiebreak, so even if duplicates ever reappear the result is reproducible. Verified: re-uploading a file another account had already loaded leaves the count at 4,412 with **0 duplicate facts**, and recomputation is still bit-identical across all 1,323 rows. **This only surfaced because a second account did the second upload** — a seed script running as one user could never have shown it | Resolved |
| 2026-09-03 | **Import tab built — check and load share one code path** | `POST /api/import/check` and `/load` plus `GET /api/import/batches`, and an Angular tab at `/import`. **Check runs the identical server function as load and returns before the last statement.** A validator written separately from the loader eventually disagrees with it, and the disagreement reaches users as *"it passed the check and then failed to load"*, which destroys confidence in both | Three behaviours worth keeping: the scope pickers are a **cross-check, not routing** — `_META` says which profile a workbook is for, and a mismatch is refused rather than trusted, because uploading the right file under the wrong sector would key real values to a real profile and look entirely plausible afterwards; **review copies are detected, not declared**, from `_META.protection`, so an uploader need not know which kind they were sent; and **nothing loads partially** — a file with one negative value in a non-signed variable was refused with the row, column and value named, `batchId: null`, and the row count unchanged at 4,412. Import requires a signed-in account (NFR-4) even though the map does not | Resolved |
| 2026-09-04 | **QA pass: a schema addendum was written, documented, and never registered in the script that applies them** | An independent QA run against a fresh database failed at `seed_central.py` with `column "value_kind" does not exist`. `schema_signed_values_addendum.sql` was written on 3 Sep and named in `RUN_LOCALLY.md`'s **manual** fallback order, but never added to `apply_native.ps1` — so the automated path everyone is told to run produced a schema the documented next step could not use. An audit of `design\database\*.sql` against the script found it was the **only** unregistered file | Registered as step **4b**, after the seed. That exposed a second-order trap the QA pass had missed only through luck of ordering: at 4b, `THREE_DAY_CUMULATIVE_RAINFALL` **does not exist yet** — the panel introduces it later — so the addendum's UPDATE matched nothing and the variable silently stayed `absolute`, which would have rejected every negative value in 18 profiles at the next seed. Proven on a cold build: after 4b only `OCCURRENCE_WARM_DAYS` was signed. `apply_province_panel.py` now sets `value_kind` **on the INSERT** for the variables it creates, plus a follow-up UPDATE so a re-run repairs a row created before the column existed. Same cold build after the panel SQL: both variables signed | Resolved |
| 2026-09-04 | **A test suite keyed to a profile CODE skipped every test and reported green** | `tests/test_engine_refusal.py` hardcoded `PADDY_DROUGHT_CEN_V5`. A profile code carries its version and `save_profile_weights()` mints a new one on every save, so the code points at a version any colleague's save has already retired. `_V5` existed only on the machine it was written on, where `panel_central.sql` had been re-run five times; on a freshly seeded database only `_V1` exists, the lookup returned nothing and all four tests **skipped**. **A skipped test reports green.** `pytest-asyncio` was also missing from `requirements-dev.txt` — without it the tests ERROR rather than fail, which reads as *the tests are broken* rather than *the tests did not run*. `routers/profiles.py`'s docstring makes exactly this argument about URLs; it was not carried over to the tests | Re-keyed to the **scope** (province / sector / subsector / hazard), which is stable across versions, with a skip message that names the scope and asks whether Central is loaded. `pytest-asyncio` pinned in `requirements-dev.txt` and `asyncio_mode` set explicitly in a new `pytest.ini` rather than relying on a default that has moved between releases. Verified: the documented command now reports **4 passed**, not 4 skipped | Resolved |
| 2026-09-04 | **A CSV renamed to .xlsx returned 500; and half the map's dropdown combinations 404ed** | Two more from the same QA pass. (1) `openpyxl.load_workbook()` had no guard, so `zipfile.BadZipFile` propagated to a 500 — an unreadable upload is a **refusal with a reason**, not a server error; a 500 tells the person nothing about what they did wrong. (2) `/api/reference/taxonomy` was built specifically to stop the dropdowns offering combinations that do not exist, and solved **half** the problem: sector→subsector was scoped to real profiles, hazards were returned as one flat list applied to everything. **15 of 48 combinations 404ed** — Coconut+Landslide, every Livestock subsector+Landslide, Transportation+Drought | (1) The reader catches the open failure and returns it as an error naming the cause, so the router's existing refusal path handles it; verified over HTTP as **200 with `ok:false`, `batchId:null`**, not 500. (2) Hazards now come **per sector and per subsector** from the same DISTINCT over active profiles; both the map filter bar and the import tab narrow their hazard list and re-point a selection that has become invalid. Verified by enumerating every combination the taxonomy offers and requesting each: **33 offered, 0 404s** — matching the 33 active Central profiles exactly | Resolved |
| 2026-09-04 | **The weights editor had never worked, in two independent ways** | QA findings 1 and 2. (1) `encodeProfileScope()` appended `period`, sending a **five**-segment scope where `parse_scope()` takes four — every request 400ed, for every profile. The backend settled this on 11 Aug and its docstring explains why a profile has no period (periods belong to values and results, SRS §2.5); `profile.model.ts` was never aligned, and its own comment admitted it was "a reasonable placeholder ... align it with whatever Stage 2 actually expects". (2) Behind that, the shapes did not match either: the router emitted **snake_case** while the models declared camelCase, and returned one `variables[]` where the component read `hazardVariables`/`exposureVariables` — so `.map()` on `undefined` would have thrown had a request ever succeeded | `routers/profiles.py` moved to camelCase and to the domain-split shape, because the frontend models, SRS §9 and the newer routers all already agreed with each other — profiles.py was the outlier. `period` dropped from `ProfileScope`, `encodeProfileScope()` and `scopeFromQueryParams()`; `/entry` still passes it as a separate query param for the screens that need it. `relationship` stays the catalogue enum in the contract and is mapped to the legacy '+'/'-' **at the view**, not in the API. The api-client flattens the editor's two blocks into the API's one `items` array at the boundary — one documented translation, mirroring the one profiles.py already does for the database function. Verified end to end: **GET returns the right shape** (totals hazard 100/exposure 100, 6 hazard + 10 exposure rows), the old five-segment scope still **400s**, **PUT saved V6** with its panel note, and a domain short by 5 was refused **422** in the rule's own words. `toEditableRows` also gained a null-guard, so a future mismatch is an empty table rather than a blank page | Resolved |
| | | | | |

---

## 4. Maintenance rule

When a function finishes: set its **Executed** cell to ☑, fill **Date** + **Output**, update
the **§1 summary counts** and the **Current phase** field, and add any problems to **§3**.
Keep this file and `PROJECT_GUIDE.md`'s Status log in sync.
