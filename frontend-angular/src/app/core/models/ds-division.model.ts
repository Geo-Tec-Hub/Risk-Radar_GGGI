/** Properties carried by each feature in the boundary GeoJSON (see BoundaryService). */
export interface DsDivisionProperties {
  readonly ds_code: string;
  readonly ds_division: string;
  readonly district: string;
  readonly province: string;
}
