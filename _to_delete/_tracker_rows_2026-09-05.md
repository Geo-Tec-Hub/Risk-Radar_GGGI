| 2026-09-05 | **Per-sector write scope — a province was never a narrow enough boundary for someone who writes data** | Milinda, setting up a data-entry agency account: *"there is no specific sector for data entry office — he can import any data set, but if you look at the files they are specific for sector or subsector and hazard. Same is true for external experts."* He is right, and the timing matters: since 3 September the official track is **latest-import-supersedes**, adopted to fix a duplicate-rows defect. Before that change a wrong-sector upload produced a harmless duplicate. After it, the upload **overwrites**, silently, and the map recomputes as though nothing happened. Every agency in Central shares one province, so §3.2's provincial binding was the only thing standing between a livestock officer and the paddy figures | **Where the boundary actually goes, measured on Central's loaded data rather than assumed:** exposure is 97 variables / 3,920 facts, each used by ~1.9 profiles — genuinely sector-specific, so a sector grant governs 89% of the data cleanly. Hazard is 12 variables / 492 facts, each used by **~13.5 profiles** — SPI, warm days, rainfall and event counts belong to the Met Department and the DMC, not to Agriculture or Livestock, and every workbook carries both. A sector grant alone would therefore still let a paddy officer overwrite the climate figures thirteen other sectors depend on. Hence **two separate things**: `user_write_scope` (sector, optionally narrowed to one subsector) and a distinct `app_user.may_write_hazard_domain` flag. Milinda chose this over restricting whole templates, and chose sector-with-optional-subsector granularity. **Deliberately NOT scoped:** reads (the map is public, FR-12.7 — an officer who cannot see other sectors cannot sanity-check their own); hazard *type* (the agency that owns paddy owns paddy-drought and paddy-flood alike, and only the climate columns differ); province (already singular and enforced). `schema_write_scope_addendum.sql`, step **7b** in `apply_native.ps1`. One function answers the question — `may_write_profile(user, province, sector, subsector)`, default deny, admin bypass — and the API asks it rather than reimplementing it, so the rule cannot drift between an endpoint and the database. Enforced at both write paths: `load_template.py` refuses an out-of-scope workbook and, separately, refuses a workbook carrying hazard columns without the grant; `PUT /profiles/{scope}/weights` refuses out of scope. Verified live: out-of-scope Livestock import refused, in-scope Tea import refused for its hazard columns, granting the flag unlocked Tea (328 values, 41 divisions) while Livestock stayed refused | Resolved |
| 2026-09-05 | **`save_profile_weights()` accepted a payload naming a variable that does not exist, and reported success** | Found while testing the scope enforcement above. This went in and came back **200**: `{"items":[{"indicatorCode":"X","domain":"hazard","weightPct":100}]}`. It retired `TEA_FLOOD_CEN_V5` — a good version with eleven memberships — and installed an active **V6 with zero**. The profile stopped being computable and an expert panel's entire weighting vanished from the active version, with no error anywhere and no trace but the version number going up. **Three independent holes, each sufficient alone.** (1) The INSERT ends `JOIN indicator_catalog ic ON ic.code = x.code`, so an unknown code contributes no row and the save still succeeds. (2) The 100% rule groups by domain **over the items supplied** — send no exposure rows and there is no exposure group, so nothing is compared to 100. (3) Following from both, omitting a variable the profile carries drops it, where this project's standing rule is that exclusion is a recorded decision and never an inference (SRS §2.4, FR-4.9). **This is the second defect of exactly this shape in this one function** — 15 August's reset every membership to `agreed`, discarding exclusions. The pattern worth naming: *this function validates what it was sent and never what it was sent against* | `schema_weights_save_completeness_addendum.sql`, registered as step **5c** and required to run immediately after 5b, because it **splices** `PERFORM assert_weights_payload_complete(...)` into the `save_profile_weights()` body it finds in `pg_proc` — run it before 5b and there is nothing to splice; re-run 5b afterwards and the splice is overwritten. It raises if the splice point is not found rather than silently doing nothing. The checks run **before anything is retired**, so a refused save leaves the active version untouched — the 5 September damage was not the refusal that never came, it was that a good version had already been retired by the time anything went wrong. Fixed at the database, not the router, so every caller is covered; `routers/profiles.py` already translates `RaiseError` to 422 with the message unchanged. `TEA_FLOOD_CEN` restored (V6 deleted — it held nothing and no result rested on it — and V5 reactivated; 41 divisions recompute, `profileVersion: 5`). **`backend/tests/test_weights_save_completeness.py`** — six tests, keyed on the scope and never on a profile code, per the lesson from QA #6: an unchanged round trip is accepted (positive control), each of the three holes is refused **and names what is wrong**, removal by sending `rejected` still works (so the checks have not made a legitimate edit impossible), and a refused save leaves the active version intact. All pass, alongside the four Stage 4 refusal tests | Resolved |
| 2026-09-05 | **Interest areas at registration, granted at approval** | The scope work above needs a way for a scope to be *asked for* and *granted*. Milinda: *"shall we ask them to pick the interest area so that admin can approve it — one can also select multiple sectors, hazards if they wish"* | **Request and grant are kept in two different places, on purpose.** `app_user.requested_scope` (JSONB, codes not ids, so it stays readable years later) holds what the applicant asked for; `user_write_scope` holds what an administrator granted. Writing the request into the grant table at registration would make an unapproved wish indistinguishable from an approved permission, and the grant table is what every check reads. Codes are validated at **request** time, not approval time — an applicant who mistypes a sector can fix it while the form is in front of them; an administrator meeting the same mistake three days later can only guess what was meant. `app/scope.py` is the one definition of the shape, because the same thing appears on the registration form, the approval screen and the amendment endpoint, and a shape defined three times drifts. **The approval refuses to default.** `POST /admin/registrations/{id}/approve` **requires** `scopes` for `data_officer` and `expert` and never fills it in from the request: an approval that silently grants whatever was asked is not a decision. The error names what the applicant asked for, so the administrator is not sent looking for it. The grant runs **last and in the same transaction** as the role and province, so a scope that cannot be granted leaves the account `pending` rather than active-but-unable-to-write — verified. `PUT /admin/users/{id}/scope` amends later (someone changes teams); it **replaces** rather than adds, so what is on the screen is what the account ends up with, and an empty list is a valid decision that revokes everything. **A second reference endpoint, `GET /reference/interest-areas`**, deliberately different from `/taxonomy`: `/taxonomy` answers *what can I look at* and is scoped to profiles that exist, which is right for the map and wrong here — nobody could ever ask to be the first officer for a sector, because the sector would be invisible until data existed and the data cannot exist until someone is granted the sector. **Frontend:** the registration form asks agency and expert applicants where they work (checkboxes, not a multi-select — this is the part applicants are most likely to get wrong, and a multi-select shows only what happens to be highlighted), with the shared climate variables ticked separately. The admin queue shows the request as **text** and starts the boxes **empty**, with one "Grant what was asked" button: pre-ticking the request would hand the decision straight back and satisfy the server's guard with a form nobody read. A whole-sector tick alongside one of its subsectors is refused at both ends and explained in place — it is harmless to the permission check but almost always means a box was ticked by accident, and approving the list as written would grant the whole sector unnoticed. Verified end to end against the replica: five registration refusals with their own messages, approval refused without a scope, approval granting **less** than requested, the grant gating `may_write_profile` (Paddy yes, Tea no, Coconut no, right sector wrong province no), amendment widening it, and revocation stopping the writes. `node tplcheck.mjs` clean — and confirmed non-vacuous by breaking a binding on purpose and watching it fail | Resolved |
| 2026-09-05 | **A malformed stored password hash was a 500, not a failed login** | Noticed while setting up a test account: `verify_password()` caught only `VerifyMismatchError`, so a row whose `password_hash` was a placeholder — a seeded or hand-inserted account — raised `InvalidHashError` straight out of the login route. It **fails closed**, so it was never a way in. But it is the wrong answer: the account cannot authenticate, and the honest report of that is *these credentials do not work*, not *the server is broken* — and a 500 sends whoever is debugging it to entirely the wrong place | Caught and returned `False`, with a warning logged (the row needs repairing) and the hash itself never logged. **`InvalidHashError` is NOT a subclass of `VerificationError` in argon2-cffi** — it derives from `ValueError` — so catching the base class alone would have looked like a fix and changed nothing; checked before trusting it. Verified live: unparseable hash → 401 with the normal message, valid login → 200, wrong password → 401, warning present, hash absent from the log | Resolved |
