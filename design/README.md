# Design — Phase 0 artifacts

Finalize everything here **before** any code (see PROJECT_GUIDE.md §4, Phase 0 gate).

| Folder | Holds | Component |
|---|---|---|
| `architecture/` | system architecture + data-flow + agent-layer diagrams | D1, D2 |
| `database/` | ERD diagram + `schema.sql` DDL + indicator_catalog model | D3 |
| `ui/` | screen inventory, wireframes, clickable prototype | D4 |
| `srs/` | Software Requirements Spec (`.md` → `.docx`) | D5 |
| `templates/` | Excel upload templates (hazard / exposure) | D6 |

Each subfolder has a placeholder noting what goes in it and the prompt that produces it.
