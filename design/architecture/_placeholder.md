# architecture/ — to be produced (D1, D2)

Expected artifacts:
- `system-architecture.svg` — frontend ↔ FastAPI ↔ Postgres(PostGIS+pgvector) ↔ agent layer, 3 tracks, SSP + monitoring.
- `data-flow.svg` — raw indicator → normalization → hazard/exposure index → vulnerability=f(E,H) → SSP → impact → monitoring.

Produce with the **figma-generate-diagram** plugin or the inline visualizer.
See PROJECT_GUIDE.md §4 D1/D2 for the exact prompts.
