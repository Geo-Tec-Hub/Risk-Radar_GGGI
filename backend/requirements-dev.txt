-r requirements.txt

iniconfig==2.3.0
packaging==26.3
pluggy==1.6.0
Pygments==2.21.0
pytest==9.1.1
# The engine tests are async (asyncpg). Without this, all four ERROR with
# "async def functions are not natively supported" - they do not fail, they do
# not run. Found by the 4 Sep QA pass, which had to install it by hand to get
# past the documented command.
pytest-asyncio==1.3.0
