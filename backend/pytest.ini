[pytest]
# Explicit rather than relying on a default that has moved between releases.
# The engine tests are async; with pytest-asyncio absent or misconfigured they
# ERROR rather than fail, which reads as "the tests are broken" instead of
# "the tests did not run".
asyncio_mode = strict
testpaths = tests
